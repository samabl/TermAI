# TermAI Harness — 项目总纲 v1.0

> **本文档是 TermAI 项目的唯一权威设计总纲（Harness）。**
> 它由 Orchestrator 汇总并仲裁 10 份独立资深角色评审而成：产品（01）、UX/用户研究（02）、设计系统（03）、终端内核（04）、前端/UI（05）、后端/云平台（06）、AI/Agent（07）、安全隐私合规（08）、开发者体验与插件生态（09）、CTO/工程（10）。
> **效力**：角色文档之间、角色文档与本文冲突时，一律以本文为准。文中标记为「不可协商」的条目，变更需 TSC 2/3 多数 + 90 天公示。
> **产物结构**：仲裁决议 AR-01…AR-20（冲突裁决）→ 决策登记 DC-01…DC-40（合并去重后的可执行结论）→ 架构与预算 → 路线图与门禁 → 风险与待决议。

---

## 0. 如何使用本 Harness

1. **先读本文，再读领域规格（docs/spec/），最后按需下钻角色原文（docs/roles/）。** 角色原文保留全部反方意见与推理过程，是 ADR 的一等证据。
2. **任何实现工作开工前**必须能指出它落在哪个 Phase（§7）、满足哪条 DC、受哪条预算（§5）与门禁（§8）约束。
3. **发现冲突** → 先查 §2 是否已裁决；未裁决的走 RFC → ADR（docs/adr/），不得由单个模块自行决定。
4. **不可协商清单**（违反即架构事故）：
   - AI 不进入 PTY→像素热路径；内核不依赖 AI、不依赖网络（§2 AR-03）。
   - 终端字符网格不由 WebView 渲染（§2 AR-01）。
   - 破坏性与外发型操作的确认**不可由配置关闭**（§2 AR-06）。
   - 插件不得拦截/改写 PTY 字节流，不得获得 Node/原生句柄（§2 AR-07）。
   - PTY 流、文件内容、命令输出、密钥材料**永不进入服务端**（§2 AR-11）。
   - secret 永不进入模型上下文、日志与遥测（§6）。
   - 核心链接边界内不得出现 GPL/AGPL/SSPL（§2 AR-21）。

---

## 1. 北极星与产品第一性

### 1.1 定位（一句话）

> **TermAI 是一个把「从报错到修复」的全部上下文交给可审计 AI 的现代终端：终端模拟器 + 结构化上下文总线 + 内嵌 Agent + 插件生态。**

它不是「带聊天框的终端」，也不是「终端形态的 IDE」。差异化不在模型接入，而在**AI 能读到什么（结构化上下文）、能否安全写回（能力 + 审批）、能否审计与回滚（审计链 + 撤销快照）**。

### 1.2 三条产品公理（冲突时的最高裁决依据）

| # | 公理 | 推论 |
| --- | --- | --- |
| A1 | **终端是信任基础设施** | 一次生产误操作造成的信任损失无法用功能弥补 → 默认安全、默认 dry-run、可审计优先于自动化率 |
| A2 | **终端的本命是延迟、正确性、可脚本化** | VT/宽字符/IME/剪贴板/管道有一处不对，AI 再好也没人用 → 兼容性与性能是入场券 |
| A3 | **AI-native = 状态结构化，而非接了大模型** | 会话、命令块、退出码、文件系统、Git、容器状态必须是 AI 可读写的结构化对象 |

### 1.3 用户与 JTBD

| Persona | JTBD | 定位 |
| --- | --- | --- |
| SRE / 平台工程师 | 线上故障时快速定位并**安全**执行修复 | **Primary** |
| 高级软件工程师 | 在终端内闭环完成构建—调试—提交 | **Primary** |
| 数据 / AI 工程师 | 长任务、环境与 GPU 资源管理 | Secondary |
| CLI 重度用户 / 终端爱好者 | 管道、脚本、配置、主题、插件 | Secondary |

明确**不做**「零基础学终端」教学市场（AR-15）；但零配置即是一条合格的普通终端（渐进披露，§2 AR-16）。

### 1.4 指标

| 类型 | 指标 | 目标 |
| --- | --- | --- |
| **北极星 NSM** | Accepted AI Actions per WAU | ≥ 5（上线 6 个月） |
| 护栏 | 危险命令误执行率 | < 0.5% |
| 护栏 | L2/L3 未审批执行次数 | **= 0** |
| 护栏 | AI 关闭时终端核心可用性 | 100% |
| 先导 | AI 建议接受率 | 25%–40% |
| 留存 | D7 / W4 | ≥ 40% / ≥ 20% |
| 信任 | 出网 secret 泄露事件 | 0 |

---

## 2. 仲裁决议（AR-01 … AR-20）

> 格式：**分歧 → 决议 → 理由 → 反方保留与复议条件**。所有被否决方案的反方论点完整保留在对应角色文档第五节。

### AR-01｜UI 与渲染技术栈（全原生 vs 混合 vs 全 WebView）
- **分歧**：01 主张 A（全原生 UI 工具链）；02/03/04/05/10 主张混合；共同否决 C（Electron + xterm.js）。
- **决议**：采用 **D = Native Grid + Web Shell 混合架构**。终端字符网格与外壳 chrome（tab/分屏/状态栏/命令面板/浮层）由 Rust + wgpu 自绘；AI 面板、设置、插件视图由系统 WebView 承载。
- **理由**：像素热路径必须由 Rust 直提 GPU（key-to-photon、字形 shaping、Sixel/Kitty、IME 光标）；富交互 UI 自研 ROI 极低。
- **反方保留（01）**：双 UI 栈带来永久一致性成本、UI 迭代慢、人才面窄。**复议条件**：若任一目标平台 WebView 无法达到「P99 ≤16ms 且 IME 可修正」，启动面板原生化分期评估。
- **硬边界**：网格与关键动画绝不走 Web；WebView 不得覆盖原生 IME 候选窗/右键菜单（L4）；跨层拖拽由 Core 接管指针。

### AR-02｜WebView 的地位与最低依赖
- **分歧**：10 主张 WebView 不进最低依赖、默认不加载；05 担心 Linux WebKitGTK 碎片；02 的复议门槛是 60fps / 16ms。
- **决议**：WebView 为**可选运行时依赖**。缺失或不可信时降级为纯原生文本面板（功能子集可用）；Linux 以 AppImage 自带运行时为备选。Windows 默认 WebView2 **evergreen**（安全优先），企业版提供 fixed-version 选项。
- **理由**：插件 UI 的迭代速度值这个代价，但终端的可用性不能抵押给系统组件。

### AR-03｜AI 运行时与内核的关系（本轮最关键的语义仲裁）
- **分歧**：04「AI 不得出现在 PTY→像素关键路径，必须是 sessiond 之外的消费者」；07「AI 绝不进内核，只消费结构化事件」；10「Agent Runtime 是一等内核组件，不得做成插件」。
- **决议（三分）**：
  1. **产品层**：Agent Runtime 是**第一方一等组件**（随客户端同版本发布、默认集成、可审计、非插件），采纳 10 的安全底线。
  2. **进程层**：Agent Runtime 运行在**独立进程**（termai-agent + watchdog），内核不链接任何 AI SDK、不持有网络句柄、不因 AI 关闭产生任何开销，采纳 04/07。
  3. **契约层**：内核只承诺「结构化 Session Log + 版本化能力协议」；AI 通过只读订阅 + 受控写入访问 Context，写 stdin 必须显式授权。
- **理由**：「一等组件」是产品与安全属性，「进程内」是实现属性；两者不可混谈。此解同时澄清了 10 的 D-09 与 04/07 的边界。
- **反方保留（07）**：若硬隔离后仍能做到零开销，可重评集成度。**复议条件**：AI 关闭时 P95 时延劣化 <1%，且无共享地址空间。

### AR-04｜上下文模型与结构化事件总线（含性能红线）
- **分歧**：06「结构化事件总线是否过度抽象伤性能」；04/05 担心热路径序列化开销；07 要求稳定 schema。
- **决议**：
  - 唯一真相 = **append-only Session Log**（分段 + 校验和）；SQLite(WAL) 只作**可重建**的派生索引；大对象走内容寻址 CAS。
  - 进程内通信走 trait 直连 + 二进制快照/delta；跨进程才走 termai-ipc（长度前缀 + 版本/能力协商）；**热路径禁用 JSON/gRPC**。
  - 结构化事件**惰性生成**：无消费者时零开销；有消费者时按订阅裁剪。
  - **schema 归属**：由 termai-core 的 IDL 定义并版本化，内核与 agent 共同维护（含 OSC 133/633 命令边界、退出码、cwd、耗时、时间戳）；破坏性变更走 capability 协商，兼容窗口 ≥ 2 个 minor。
- **理由**：可裁剪、可脱敏、可回放、可评测是 AI 的前提；字符串拼接是隐私与评测的坟墓。

### AR-05｜Agent 架构形态
- **决议**：**单 Agent 主循环 + 受控子 Agent 委派**。子 Agent 仅用于可并行的检索、多仓库调查、长日志分析；多 Agent 编排框架默认关闭。
- **理由**：终端任务粒度小、时延敏感，编排开销与不确定性远大于收益。
- **复议条件**：大规模运维（Phase 4+）确有分工需求时，以显式开关启用。

### AR-06｜权限、审批与撤销模型
- **分歧**：08「危险动作逐条批准，且不可配置关闭」；02「拒绝逐条确认（盲确认率反升），改分级摩擦 + 二次输入目标名」；07「四级 + 三档授权，信任模式最多放行 L0/L1」。
- **决议**：
  - 风险分级：**L0 只读 / L1 幂等写 / L2 破坏性 / L3 不可逆或外发 / U 不可判定（按 L2 起步并说明原因）**。
  - 授权三档：once / session / rule；**L0、L1** 可被规则固化；**L2 必须每次 dry-run + 确认**；**L3 必须每次确认 + 二次输入目标名/短语 + 不可回滚徽章**。
  - **不可协商**：破坏性与外发型操作的确认不可由配置关闭；企业策略只能加强，不能削弱。
  - 采纳 02 的交互形态（分级摩擦 + 二次输入），否决「逐条弹窗」与「零确认自动执行」两个极端。
  - 撤销栈**只承诺**工作区文件与 git 可逆操作；绝不承诺 OS/容器/远端回滚，UI 必须明示边界。
- **理由**：确认疲劳使安全性反低于「无确认 + 可撤销」；但不可逆操作没有事后补偿路径。

### AR-07｜插件 ABI 与 PTY 边界
- **分歧**：01 与 09 一致主张 WASM 唯一 ABI；04 硬否决插件拦截/改写 PTY 字节；08 认为「WASM 即沙箱」不成立，必须叠加进程隔离与签名。
- **决议**：
  - 唯一 ABI = **WIT + WASM Component**（语言中立、WASI 零 ambient authority）；第三方原生 dylib 一律拒绝。
  - 第二种形态 = **trusted subprocess**（白名单：git/docker/kubectl/云 CLI），须用户显式标记，不进入市场普通分类。
  - **插件宿主是独立进程**（满足 08），WASM 是进程内子隔离层；签名 + 权限清单强制。
  - 插件对终端只有**只读镜像 + 显式注入队列 + 声明式装饰**（cell range + style token + z-order 枚举）；**永不开放帧内任意绘制，永不写 PTY/输出流**（采纳 04/09）。
- **理由**：终端正确性与延迟下界不能由第三方抵押。

### AR-08｜插件 UI 能力与开放节奏
- **分歧**：09 主张隔离 WebView 面板；05 主张「声明式优先 + sandboxed iframe 兜底」；03 担心两套视觉。
- **决议**：v1 **只开放 Tier1 声明式 view schema**（插件提交 JSON，Host 渲染）；**Tier2 隔离源 iframe 在安全红队通过后开放**（门禁：逃逸用例 0 成功 + CSP 拦截 100%）。插件 UI 只能使用 token 子集与组件白名单，保证视觉不漂移。
- **理由**：先证明安全边界，再开放表达力。

### AR-09｜配置体系
- **决议**：**TOML + drop-in + 层级合并**（default < system < user < project < env < CLI）；不内嵌 Lua/JS 配置语言；可编程配置由 config provider 插件承担；提供 termai import（kitty/alacritty/wezterm/iTerm2）与 termai config get/set/eval --json。
- **反方保留（极客向）**：配置即代码的社区吸引力被牺牲。**复议条件**：若社区压力足够，以「Lua 作为插件作者语言」而非配置语言回归。

### AR-10｜许可与商业模式（**已被 AR-21 取代**，分歧记录保留）
- **分歧**：01/03/05/07/09/10 → Apache-2.0；02 → 内核 Apache + 编排 AGPL；06 → 本地 Apache + 云端 BSL 1.1；04/08 → MIT/Apache 双许可。
- **决议**：
  - **核心、SDK、协议 = Apache-2.0**（含专利授权；最终单一许可证由法务在 MIT/Apache 二选一确认，禁止双许可并存造成歧义）。
  - **云端商业服务**（同步、团队、企业策略、托管 AI、私有 registry）= **闭源专有**。**不采用 BSL/SSPL**：其信任摩擦与企业法务成本高于收益；改用「闭源服务 + 开放协议 + 开放客户端」。
  - **明确拒绝** AGPL/GPL 进入核心链接边界。
  - **插件公共市场 v1 零抽成、无 DRM**（采纳 09）；收入来自企业私有 registry、Verified 认证与合规能力。抽成政策变更需 TSC 决议 + 90 天公示。
  - **主题格式与主题商店永久免费开放**（采纳 03）。
- **反方保留**：06 认为放弃抽成损失直接收入；02 认为编排层缺少传染性保护。**复议条件**：商业数据证明公共市场抽成为主要收入来源（>30%）时重议，且不得追溯已发布插件。

### AR-11｜云端边界与 AI 网关
- **分歧**：06「控制面-only、服务端 prompt 字段 = 0」vs 07「云端评测与跨用户缓存需要数据」。
- **决议**：
  - **硬底线**：PTY 流、文件内容、命令输出、密钥材料永不进入服务端；客户端启动不得有阻塞式云调用。
  - AI 网关默认 **只路由与计量**，BYO-Key 直连；仅托管额度通道经我方转发并 UI 明示；计费只认托管通道。
  - **服务端不落 prompt/completion（CI 静态强制字段数 = 0）**。
  - 评测真值仅来自：(a) 用户显式同意并脱敏的会话捐赠（opt-in、可撤回）；(b) 合成任务集。**不做跨用户 prompt 缓存**；同用户缓存须显式开启。
  - 同步仅覆盖 CRDT 小文档（设置/片段/工作区元数据）；历史默认仅本地，跨端备份可选且端到端加密。
  - 协作传输优先 P2P/WebRTC + 中继；协议统一 Connect(gRPC/JSON) + SSE，WebSocket 仅双向协作。

### AR-12｜遥测与隐私默认值
- **决议**：**默认关闭（opt-in）**；崩溃上报独立开关；**内容零采集**（无命令文本、路径、代码、域名）；仅本地聚合计数 + 采样；客户端侧脱敏后才允许上报。所有出站请求必须可分类、可审计、可在 UI 回看。
- **反方保留（增长/PM）**：产品决策数据稀疏，A/B 与留存分析系统性弱化。**接受该代价**；以 12 人常驻 dogfood + 每版本 8 人可用性测试 + 社区调研部分补偿。

### AR-13｜多路复用与会话持久化
- **决议**：自研 **sessiond** 为会话唯一真源（跨平台、结构化上下文、可多端 attach）；tmux 以「键位兼容 + control-mode 兼容客户端」共存，**不强制替代**。UI 崩溃不影响进程；重启可重建最后 checkpoint + 尾部段。
- **诚实声明**：**屏幕可恢复，进程不可恢复**，文档与 UI 必须明示。
- **持久化默认值**：结构化会话元数据（命令边界、退出码、cwd、时间戳、报错摘要）默认持久；**完整原始输出 scrollback 持久默认关闭**，可选开启（可加密）。

### AR-14｜终端画质（subpixel AA）
- **分歧**：03 明确放弃 GPU subpixel AA；内核/Windows 用户观感可能受损。
- **决议**：默认**灰度抗锯齿 + hinting 关闭 + 严格网格对齐**（误差 ≤0.5px），提供「锐利/柔和」两档；**不承诺 ClearType 式 subpixel AA**，写入已知问题清单。主字体 JetBrains Mono（ligature 默认开、可关）+ Nerd Font 图标回退 + CJK 独立回退栈。
- **理由**：GPU subpixel AA 与透明背景、缩放、非整数 DPI 根本冲突，换来跨平台画质不一致。

### AR-15｜目标用户与市场范围
- **决议**：Primary = SRE/平台 + 高级工程师；**不做零基础教学市场、不做课程式引导、不做欢迎向导**（首屏即输入、零弹窗）。
- **反方保留（增长）**：放弃最大教育市场，规模故事变难。**复议条件**：若留存实验证明情境化教学显著提升 Secondary 转化，可作为独立「场景模板」而非教学模式回归。

### AR-16｜AI 交互范式与输出视觉语法（**「侧边面板在右侧」一句已被 AR-23 修订**，入口顺序不变）
- **决议（顺序固定）**：**内联 Agent（?? 前缀）为第一入口 → 侧边面板为主场（宽度 ≤40%，可收成徽标）→ ghost text 仅补全（不生成副作用命令）→ 全屏 Chat 仅面板次级模式 → 整行灰字预测默认关闭**。
- **视觉语法**：AI 输出必须经结构化 schema（Inline Hint / Command Explain / Diff Block / Diagnosis Card / Risk Ladder）渲染，**禁止自由 markdown 或原始 HTML 直出**；行数 >40 或被折叠的 diff 不计入终端 scrollback。
- **错误是一等公民 artifact**：永不吞 stderr、永不美化退出码；保留原始输出 + exit code + 所在 Pane。

### AR-17｜AI 差异化价值排序
- **决议**：**故障诊断 > 上下文感知命令生成 > 大规模运维（Phase 4+）**。明确不做通用聊天、代码编辑器、自然语言 Todo。
- **理由**：诊断是 AI 独有、补全类产品做不到；命令生成会被 shell 补全蚕食，不足以作护城河。

### AR-18｜VT 引擎自研程度
- **分歧**：04「复用 vte 作基础状态机（fork-pin）+ 自研扩展层，不自研 ESC 解析器」；10「不 fork 现有终端代码，clean-room 自研，以行为测试集为规格」。
- **决议（分阶段）**：Phase 0–2 以 **vte 作为依赖**（非 fork）实现基础 ESC 状态机，置于 termai-vt 的 trait 边界之后；OSC/kitty/iTerm2/Sixel/grid 语义层自研。同时立项 **clean-room 解析器评估**，仅当 vte 无法承载新协议或上游停滞时按 trait 边界替换。锁定版本 + 记录上游差异。
- **理由**：既不重复已验证的苦力劳动（04），又保住架构自主与可替换性（10）。

### AR-19｜性能预算口径统一
- **分歧**：02 冷启动 ≤100ms；04/05/10 ≤150ms；10 延迟 P99 ≤16ms；04/05 P99 ≤8ms；对比度 03 ≥7:1 vs 02 ≥4.5:1。
- **决议**：统一为「**发布门禁**」与「**目标值**」两列（见 §5），避免多口径互相掩护；任何单项指标的最坏场景以门禁为准，达标余量记为工程债。

### AR-20｜可访问性与国际化承诺边界
- **决议**：承诺 **UI 层**（Tab/Pane/面板/设置）对 NVDA/VoiceOver 全流程可达（WCAG 2.2 AA）；**不承诺语义化 TUI**，只承诺 raw 输出朗读 + 应用模式切换。核心任务键盘完成率 100%，命令面板功能覆盖率 100%（CI 校验动作表与菜单差集为空）。i18n v1 即用 ICU MessageFormat；RTL 镜像**不阻塞 v1**，但网格内 bidi 必须走 Unicode Bidi 算法。
- **诚实原则**：所有能力边界必须在产品内明示，不允许用模糊措辞掩盖。

### AR-21｜许可与开源的最终定位（项目发起人决策，取代 AR-10 的许可与商业形态部分）
- **背景**：AR-10 原裁定「核心 Apache-2.0 + 云端闭源专有」（Open Core）。项目发起人已明确：**TermAI 是开源项目**，且范围是**全栈开源（客户端 + 云端服务）**。
- **决议**：
  1. **许可证**：客户端、服务端、SDK、协议、工具链统一采用 **Apache-2.0 OR MIT 双许可**。CI 以 SPDX 表达式校验（`Apache-2.0 OR MIT`）；仓库根提供 LICENSE-APACHE 与 LICENSE-MIT，不强制在每个源文件加头。
  2. **开源范围**：**全栈开源，不存在「闭源云端模块」**。官方商业形态是托管服务、支持订阅与合规/企业服务，**其源码同样公开**；若存在企业专有定制，必须以独立插件或独立仓库形式存在，不得回流进核心。
  3. **链接边界**：核心链接边界内**禁止 GPL/AGPL/SSPL**（保护下游与企业采用）；**MPL-2.0 / EPL-2.0 等弱 copyleft 的准入由 ADR-0015 裁定**（见 §11.2 CR-08）。「链接边界」的 CI 可执行定义同样由 ADR-0015 给出。
  4. **不可变的商业承诺**（本项不随许可变更）：本地功能永不被远程锁死；插件公共市场 **v1 零抽成、无 DRM**；**主题格式与主题商店永久免费开放**；不出售用户数据。
  5. **风险自认**：全栈开源意味着竞品可整体托管。护城河由**生态规模、体验与上下文质量**承担，而非许可证（采纳 10 的 D-08 立场）。
- **取代关系**：本决议取代 AR-10 中「核心单一许可证」「云端闭源专有」两部分；AR-10 的各方分歧与反方意见作为历史证据保留，不得删除。
- **反方保留（06 云端架构师）**：全栈开源下收入模型更依赖执行而非许可保护。**复议条件**：若 18 个月内官方托管收入无法覆盖控制面成本，可复议把**运营侧工具**（部署/计费/SRE 工具，非用户数据面、非客户端）转为专有——但不得触碰第 4 条商业承诺。
- **变更程序**：许可与开源范围的任何变动需 TSC 2/3 + 90 天公示（AGENTS.md §5）。

### AR-22｜产品视觉语言 v2 与信息密度规则（发起人接受 ADR-0016，取代 DC-10）
- **背景**：v1 视觉（暗底 + 霓虹冷青 + 高密度 + 无侧栏）被发起人否决；确认采用 Netcatty 取向的视觉语言，并提出六条要求（统一、放松布局、小 UI 去文字、终端自动换行、Agent 跟随会话且支持跨会话、减少滚动条）。细则见 **ADR-0016** 与 `docs/spec/02`。
- **决议**：
  1. **视觉语言**：macOS 风格浮动圆角窗 + **azure 蓝**主色（深 `#4A9EFF` / 浅 `#245BC9`）+ 卡片化 + **浅色为一等主题**；间距标尺 4/8/12/16/24/32/48；圆角 8/10/14/999；图标统一内联 SVG、stroke 1.5、仅 16/20/24 三档；**emoji 仅允许出现在 AI 回答正文，不得用作 chrome 图标**；按钮只允许 primary/secondary/tertiary/danger 四级。
  2. **信息密度**：卡片 padding ≥16（推荐 20）、卡片间距 ≥16、区块间距 ≥32；UI 行高 1.5–1.6（终端网格维持 1.25）；顶栏次要动作收进「更多」菜单；状态栏高度 ≥28px。
  3. **小 UI 去文字**：次要按钮一律图标化，附 `title` + 自定义 tooltip（延迟 400ms、focus 亦显示、不遮挡触发元素、Esc 关闭）；必须带 `aria-label`。仅主导航、主按钮、分段控件保留可见文字。
  4. **终端软换行**：默认开启；超列宽在视觉上折行、**无横向滚动**，且**不改变字符内容、网格列数与复制结果**；提供开关（超长单行折行会降低单屏信息量，故必须可关）。
  5. **Agent Scope（细化 DC-03）**：Agent Session 是"可提升作用域的上下文对象"——**本会话**（默认，跟随激活的 PTY Session，按 AR-06 常规分级）/ **本工作区（只读跨会话，P2）** / **多选会话（写操作 P3 起）**。跨会话引用默认只读；**跨会话写操作风险上调一档**（L1→L2、L2→L3）、**不得被 session/rule 白名单固化**、审批卡必须列出全部目标会话并要求二次输入目标名、审计必须记录目标会话集合。
  6. **减少滚动条**：全局 overlay 滚动条（静止不可见或极淡、hover 显现、宽 6px、无轨道、不挤压内容）；终端无横向滚动；AI 面板长内容改为可折叠分区（默认展开 ≤2 个）；工作区视图首屏无页面级纵向滚动。
  7. **导航收敛**：导航固定为 Workspaces / Sessions / Snippets / Themes / Plugins / Audit Log（+ Settings）；主机、凭据、Known Hosts 降级为 **Workspace 内的资源视图**。（**落点已被 AR-23 修订**：由「主导航」改为「侧栏 rail 分区」，并新增 Agent 分区。）
- **不改变的边界**：AR-01（分层）、AR-03（AI 与内核）、AR-06（风险与审批）、AR-14（画质）、AR-20（a11y 承诺）与 ADR-0013/0014/0015 全部不变；对比度门禁仍 ≥4.5:1；a11y 不放宽。
- **代价与反方保留**：图标化降低可发现性（靠 tooltip + ⌘K 命令面板补偿）；软换行降低单屏信息量（提供开关）；跨会话写扩大攻击面（P3 前不开放写）。**终端纯粹主义反方**认为宽松布局浪费屏幕——**复议条件**：若 dogfood 数据显示同任务平均耗时或会话数下降，提供「紧凑模式」而非整体回退视觉。
- **取代关系**：取代 **DC-10**（暗底高对比 + 单一冷青 accent）。docs/spec/02 按本决议重写。

### AR-23｜终端为主体、单一侧栏、设置独立窗口、新建终端即标签选择器（发起人认可 ADR-0017）
- **背景**：v3 的「终端 / 工作区」视图切换 + 右侧常驻 Agent + 顶栏两个新建按钮，被发起人要求按「终端为主体」重构。
- **决议**：
  1. **终端是唯一主体**：删除「终端 / 工作区」视图切换；工作区、主机、凭据、Known Hosts 全部下沉到侧栏面板（不占主区）。
  2. **单一左侧栏**（48px 图标轨道 rail + 内容面板）：分区 = Workspaces / Sessions / Agent / Snippets / Themes / Plugins / Audit Log，底部 Settings 与「收起侧栏」；**侧栏固定在左侧**（左/右仅作设置项，默认左，供 RTL 与个人偏好）；可收起（Mod+B），收起时保留 48px rail 以便「一眼看出怎么展开」。
  3. **设置独立窗口**：Settings 不再占主区，作为独立浮动窗口（自有标题栏、窗口控制、圆角与投影）打开；主题/字号/软换行等状态**跨窗口实时联动**；关闭后可从侧栏底部重开。
  4. **新建终端 = 新增标签页时选择**：tab 条的「+」打开「新建终端」选择器（本地 Shell / SSH 主机 / 容器 / WSL / 已保存会话 / 从模板；支持搜索与键盘导航）；**顶栏不再有「连接主机 / 新建工作区」**。
  5. **窗口控制真实可用且跟随平台**：macOS = 左上角圆点（红/黄/绿），Windows / Linux = 右上角扁平按钮；**位置与按钮形态随平台，窗口圆角/投影/配色仍统一**（发起人选定方案 A，否决「完全按平台渲染窗口外观」）；三态行为真实——最大化铺满视口、最小化收成可点击胶囊、关闭后在壁纸留「重新打开 TermAI」入口（不得进入死态）。
  6. **软换行关闭 = 视觉裁剪**（不做横向滚动）：与真实终端「网格由列数定义」的语义一致；横向滚动属日志查看器语义，不进终端网格。
  7. **默认主题 = 跟随系统**（系统不可判定时回退深色）；深/浅两套主题均进 P0 对比度与视觉回归门禁（**关闭 OQ-31**）。
- **修订**：**AR-16**（「侧边面板在右侧」一句作废，改为「侧栏 Agent 分区」；AI 入口**顺序**与 ≤40% 宽度约束不变）、**AR-22 第 7 条**（导航落点由「主导航」改为「rail 分区」，并新增 Agent 分区）。
- **不改变**：AR-01 / AR-03 / AR-06 / AR-14 / AR-20、AR-22 其余各项、ADR-0013 / 0014 / 0015 / 0016。
- **代价与反方保留**：Agent 由「常驻可见」变为「按需展开」，可能降低使用率（AR-16 的原始证据仍然成立）；工作区浏览失去全屏舞台，检索效率需在侧栏面板内重建。**复议条件**：若线上数据显示 AI 使用率或跨会话操作数相对 v3 基线下降 >30%，回退为「Agent 常驻右栏」或提供「Agent 常驻」开关。
- **参考实现**：prototype/termai-ui-terminal-first.html；其 deep-link 参数（#theme / #plat / #side / #sec / #sidebar / #settings / #picker / #palette / #win / #scope / #fs / #wrap）作为评审约定沿用。

### AR-24｜度量口径统一（P0 内核评审第 1 号裁决）
- **背景**：§5 的性能预算缺少**进程范围与测量定义**，导致三份内核评审无法判定（kernel/03 的 OQ-K1、kernel/06 的 OQ-PM、kernel/07 的 N-8 卡在同一点）。
- **决议**：
  1. **RSS 口径 = 核心进程组**（sessiond + 原生 UI），**不含 WebView（L2）与插件宿主**；二者独立记账：WebView 就绪后增量 ≤60MB、插件宿主空载 ≤80MB（DC-38）。同时必须报告**总内存**，不允许只报核心组（AR-20 诚实原则）。
  2. **补齐两项此前「对外承诺但无门禁」的指标**：空闲 CPU ≤1%（RM-A、聚焦、无输出、60s 均值）；**未聚焦或被遮挡时出帧 = 0 帧/10s**。**功耗**降级为人工抽测目标（不进 CI），禁止文档中出现无测量方法的功耗承诺。
  3. **每条 §5 指标必须引用一个测量定义**（位于 kernel/06）：帧时 = present 时间戳差；吞吐 = headless（sessiond-only）口径；网格对齐 ≤0.5px = 字形与 cell 网格偏移的 golden 像素测量（RM-C）。无测量方法的指标不得留在 §5。
- **代价**：门禁数量与 CI 时间上升；总内存必须公开报告，可能暴露看起来超预算的数字。
- **反方保留（03 评审）**：若把 WebView 计入 120MB，该门禁在加载态必然不可达，会迫使把 WebView 移出客户端进程。**本裁决拒绝该口径**，但承认：若未来要求「单进程总量 ≤120MB」，那属于架构变更，须另开 ADR。

### AR-25｜字节保真与一致性车道（P0 内核评审第 2 号裁决）
- **背景**：kernel/01 从一致性方向提出「100%/99% 只在 L0 parser 车道判定」；kernel/02 从保真度方向提出「F0/F1/F2 保真标签」。两者是同一件事的两面。
- **决议**：
  1. **三条车道**：L0 parser（纯状态机、字节直喂）、L1 transport（PTY / ConPTY / SSH / 容器 / WSL）、L2 end-to-end（UI 渲染）。**兼容性门禁（100% / ≥99%）只在 L0 判定**，L1 差异登记但不得污染 L0 门禁。
  2. **保真标签强制声明**：每个 hop 必须声明 F0（字节不变）/ F1（仅元数据层可改）/ F2（允许渲染层改写）。**字节缓冲层永不可写**；判定为**逐字节差分、禁止采样**，失败必须打印首个差异偏移与分类。
  3. **未登记的改写即失败**：ConPTY 差异以机器可读的 conpty-rules.toml 为准（severity=block 或 scope=data 的改写 = 门禁失败）。
  4. **P0 阻塞项**：sessiond / PTY 必须提供**字节直喂 L0 parser 的接口**；否则 Windows 与降级后端的 conformance 门禁无法判定——这是 P0 实现约束，不是可延后的优化。
- **代价**：L0 直喂接口增加一层抽象与测试成本；Windows 上字节保真没有兜底路径（NativePty 已由 ADR-0014 排在 v1 之外），只能靠登记与门禁稳定化维持。

### AR-26｜会话持久化的三层保留语义（P0 内核评审第 3 号裁决）
- **背景**：03-spec §3.7 称 Session Log 真相层「含原始字节」，而 AR-13 要求「完整原始输出持久**默认关闭**」。不消解则实现者会做出互相矛盾的存储与隐私设计。
- **决议**：
  1. **P0 元数据层（默认持久）**：命令边界、退出码、cwd、标题、resize、审计引用——不含原始输出字节。
  2. **P1 原始字节恢复窗口（默认 volatile ring）**：仅在内存或临时盘保留有限窗口用于崩溃后屏幕重建，**默认不落长期存储**。
  3. **完整历史归档（opt-in）**：用户显式开启才落盘，且必须可加密、可清除、可审计（AR-12）。
  4. **崩溃耗时口径补齐**：§8.2 原有「UI 崩溃 <2s 重连」之外，新增 **sessiond 重建 P95 ≤2s / P99 ≤5s**。
  5. **OQ-30 阶段澄清**：**P1 冻结** stdin 单写者租约的 IPC 字段与语义；**P5 才启用**跨端协作写。
- **代价**：三层保留比「一个 Log 存全部」复杂；崩溃恢复可用信息受 P1 窗口大小限制，必须对用户诚实说明。

### AR-27｜性能门禁的 PR/REL 双口径（P0 内核评审第 4 号裁决）
- **背景**：§8.1-4 写「回归 >5% 阻断合并」（单次），spec 07 §3.8.3-3 写「连续 2 夜复现才阻断」——两处口径不一致。
- **决议**：拆成两条门禁：**G4-PR**（合并阻断）= 单次有效运行回归 >5% 即阻断，与 §8.1-4 一致；**G4-REL**（发版阻断）= 连续 2 夜复现或发版前全套一次。**测量本身必须先通过可复现性自检**（同 commit 同机器指纹连测两次，verdict 不得翻转），否则判 INVALID 且不得与基线比对。
- **代价**：PR 阶段可能因噪声被拦（用 INVALID 与统计口径缓解）；两道门的判定逻辑都必须实现。

### AR-28｜跨模块契约变更与编号命名空间（P0 内核评审第 5 号裁决）
- **决议**：
  1. **接口面扩张按对外契约变更处理**：PtyBackend 由 6 元面扩到 9 元面、IPC payload 编码由「Cap'n Proto / MessagePack」改判为 **CBOR + POD 双档**、crc32c 由可选升为强制——三项均须补 ADR 追认，且新增或变更对外契约需 CODEOWNERS 双签。ADR 落地前，kernel/07 的 IDL 只落 tag 表、不落编码。
  2. **编号命名空间规则**：kernel 系列内部问题编号统一为 **OQ-<分册缩写>-NN**（OQ-VT-01 / OQ-PTY-01 / OQ-RND-01 / OQ-SES-01 / OQ-INP-01 / OQ-PM-01 / OQ-ABI-01）；**已撞号的必须回溯改名**（kernel/01 现用 OQ-K-01..16、kernel/03 现用 OQ-K1..9）。
  3. **依赖准入**：russh 等新依赖进入链接边界前必须完成 ADR-0015 白名单登记；portable-pty 明确不默认采用。
- **代价**：回溯改名会产生一批文档改动；ADR 追认与双签会拖慢内核接口定稿。

### AR-29｜输入与剪贴板的默认口径（P0 内核评审第 6 号裁决）
- **背景**：kernel/05 提出 `input.shortcut_precedence` 与 spec 02 §3.5「应用模式优先于核心快捷键」冲突——在 Windows / Linux 上 Mod = Ctrl，若应用模式优先，终端应用会吞掉 Mod+K（命令面板）与 Mod+B（侧栏），用户将无法呼出产品核心功能。
- **决议**：
  1. **优先级归属**：**终端核心快捷键在 FocusRouter 层始终优先**（否则用户无法从卡死的应用中逃生）。冲突的解决方式是**改绑按键**，而不是让出优先级。
  2. **平台化默认键位**：macOS 用 Cmd 系（Cmd+K / Cmd+B）；**Windows / Linux 默认使用 Ctrl+Shift 系**（Ctrl+Shift+K / Ctrl+Shift+B），把裸 Ctrl+字母尽量留给终端应用。文档中出现的 Mod 必须标明平台展开结果。
  3. **单一编码收口**：所有进入 PTY 的字节必须由 termai-core 的 input InputEncoder 产生（键盘 / 鼠标 / 粘贴 / IME commit / API 注入五源共用）；任何模块不得旁路直写 PTY。
  4. **IME**：preedit 只存在于原生层（L4），**不进网格、不进 scrollback、不进 PTY、不进 AI 上下文**；只有 commit 才产生一次 UTF-8 写入。候选窗锚点由渲染层提供，**永不被 WebView 覆盖**（与 AR-01/AR-22 一致）。
  5. **剪贴板**：OSC 52 **读默认 deny**；**写默认 guarded**（单行放行，多行或含控制字符需确认）；**不允许静默多行**。粘贴屏障（未括号化多行确认、ESC 201~ 序列消毒）**不可由配置关闭**——与 AR-06「破坏性操作的确认不可关闭」同源。
  6. **审计**：剪贴板与注入事件只记元数据（时间、主体、会话、方向、判定、字节数、行数），**永不记录内容、摘要或哈希**（AR-12）。
  7. **平台展开规则（对第 2 条的澄清）**：Mod 抽象在 macOS = Cmd，在 Windows / Linux = **Ctrl+Shift 整体**。因此凡在 macOS 上以「Mod+Shift+X」形式存在的动作，**不得**机械展开为 Ctrl+Shift+Shift+X，必须在 Win/Linux **重新分配**（优先 Ctrl+Alt 系），并在 **Action Registry 中显式登记三平台展开结果**。CI 必须校验展开后**零冲突**（新增静态门禁 **S10**）。文档中出现的 Mod / ⌘ 必须标注平台展开，不得用 ⌘ 作为全平台代称。
- **代价**：Win/Linux 的默认键位与 macOS 不同，文档与用户习惯需要分别维护；Ctrl+Shift 系在少数应用里仍有冲突，需要可改绑。
- **反方保留**：有观点认为应当完全尊重应用模式（更接近经典终端语义）。**否决理由**：那样用户无法可靠呼出命令面板与侧栏，产品核心能力不可达。**复议条件**：若数据显示改绑后的冲突率仍高于阈值，可提供「经典终端键位」预设，但**不得作为默认**。

### AR-30｜PTY 层四项提案指标的处置与门禁命名消歧（P0 内核评审第 7 号裁决）
- **背景**：kernel/02 提出四项【新增】指标（PTY-LAT-1 / PTY-LOSSY-1 / PTY-RSS-1 / PTY-ORPHAN-1）尚未升格；索引同时发现 `spec 07` 存在两套同形不同义的 S 编号（PR 分级 S0–S5 与设计静态门禁 S1–S10）。
- **决议**：
  1. **PTY-LAT-1 升格为 §5 门禁**：PTY 层附加延迟 **P99 ≤2ms**（测量定义在 kernel/06）。它是 key-to-photon 的**子指标**，用于在端到端超标时定位到 PTY 环节。**测量锚点 = sessiond 侧「收到输入帧 → PTY 写入返回」的耗时（不含 IPC 传输与渲染）**；具体 t 锚点由 kernel/06 落定。若 owner 主张应含 IPC，必须同时说明它如何与 key-to-photon 保持可加性（子指标 + 其余段 = 端到端），否则不得改变锚点。
  2. **PTY-ORPHAN-1 升格为 §8 验收**：孤儿进程清理 **= 100%**，且会话关闭后 **≤2s** 内回收（属生命周期正确性，不进预算表）。
  3. **PTY-LOSSY-1 不单列**：已由 AR-25 第 3 条覆盖（未登记的 ConPTY 改写即门禁失败）。
  4. **PTY-RSS-1 不单列**：已由 AR-24 第 1 条的 RSS 核心进程组口径覆盖。
  5. **S 编号消歧**：**PR 分级统一写作 PR-S0…PR-S5**；设计静态门禁保持 S1–S10。两套编号不得再以裸 S 混用。
- **代价**：§5 与 §8 各增一项；`spec 07` 需重命名 PR 分级并更新引用（历史变更记录保持原样不改写，符合不修改历史的纪律）。
- **反方保留**：有观点认为 PTY-LAT-1 与 key-to-photon 重复、不必单列。**否决理由**：端到端超标时无法判断是 PTY 还是渲染，子指标是排障必需品。

### AR-31｜内核 P0 待决项裁决（依据 00-p0-triage 的 A 档候选）
- **背景**：内核七分册仍开放 P0 级待决项 21 条；分诊文件 `docs/spec/kernel/00-p0-triage.md` 将其分为 A/B/C 三档，A 档 10 条需拍板。以下为裁决，B 档 8 条与 C 档 3 条**采纳分诊建议值**（见该文件 §3/§4），不再重复。
- **A 档裁决**：
  1. **OQ-VT-03（xterm 用例基数）→ 候选 ①**：xterm 用例 ≥2000 条，ctlseqs 每条目至少 1 个用例，真实语料占比 ≥20%。**理由**：G1 的 ≥99% 只有在用例集可复现、可解释时才可信；否则门禁数字会退化成不可审计的橡皮章。**代价**：语料建设 4–6 人周 + 长期维护。
  2. **OQ-VT-09（默认 $TERM）→ 候选 ①**：v1 默认 `xterm-256color`，随包附带 `termai` terminfo，P2 评估切换。**理由**：应用探测面零变化，同时提前铺好能力通道。
  3. **OQ-PTY-01（conpty-rules 归属）→ 候选 ①**：owner = T1 内核（termai-pty），T5 复核；**每条规则必须带 owner + expires（默认 2 个 minor）**。**理由**：`conpty-rules.toml` 是 AR-25 第 3 条的门禁真源，owner 必须与「改写登记 + 门禁判定」是同一责任体。
  4. **OQ-INP-01（§5 输入正确性行）→ 采纳候选 ② 并收窄**：§5 **已有**「输入字节等价 = 100%（逐字节）」一行（AR-29 时加入），即该行**只收可自动化的字节等价**；IME preedit 与候选窗正确性（IN-AC-04）**不进 §5**，保留在 §8.2 主题验收 + L6 矩阵 + 人工会签。**理由**：把发布阻断点押在不可自动化的截图矩阵上会拖垮 P0 出口。
  5. **OQ-INP-06（Wayland text-input-v3）→ 候选 ①**：P0 只要求 X11/fcitx5 的 preedit 可用；Wayland 缺少 `text-input-v3` 时**登记偏差 + UI 明示，不计 P0 失败**。**理由**：否则主流 Linux 桌面会被挡在 P0 之外。
  6. **OQ-INP-12（粘贴可配置边界）→ 候选 ③**：配置**只能加强不能削弱**（`ask` 可升为 `always`，不得降为 `never`）；**消毒与审计不可关闭**。**理由**：与 AR-06「企业策略只能加强」同源，且不破坏 AR-29 第 5 条「不允许静默多行」。
  7. **OQ-PM-01（安装包口径）→ 候选 ①**：按平台主格式计（Windows MSI / macOS DMG universal2 / Linux AppImage），任一超限即 FAIL；macOS 超限时改用 thin slice + 双下载。**理由**：不改 §5 数值，且给 universal2 明确兜底。
  8. **OQ-PM-04（参考机不可用）→ 候选 ①**：按路径范围判定——不触及 `term-render / term-gpu / term-vt / term-session / term-pty` 的 PR 可标 `SKIP(no_hotpath)`；触及热路径的 PR 一律不得 SKIP。**理由**：与 ADR-0014「不得用云 runner 顶替门禁」一致，且不牺牲热路径覆盖率。
  9. **OQ-PM-07（MAD 分级）→ 候选 ①**：按指标族分级（帧时 5% / 启动 3% / 吞吐 2% / RSS 1%）。**理由**：分级只改统计窗口，不改门禁数值语义；统一 2% 会让 P99 类指标随机变红，统一 5% 会掩盖吞吐回归。
  10. **OQ-PM-08（语料权重）→ 候选 ①**：分片权重**入库即冻结**，权重变更需 ADR。**理由**：权重直接定义 ≥500MB/s 的含义，冻结才可比较。
- **分诊文件提出的分组修正，处置如下**：
  - **D1**：`kernel/02 §8.1` 的 PTY-AC-09…12 与索引 L-13 仍写「未升格」，但 AR-30 已全部裁决 → **回填关闭**，不得再作为待决项。
  - **D3/D4**：`OQ-VT-12`、`OQ-INP-10`、`OQ-PM-12` 从 P0 **降为 P1**（三者自带占位方案已足够支撑 P0）。
  - **D5**：`OQ-PTY-06` 与索引 §3 的 WSL 保真级别冲突 → **以保守为准**：WSL 的保真级别**取决于其底层 Transport**——经 ConPTY 承载时按 ConPTY 登记（F1/F2 + Lossy，未登记改写即失败）；直接使用 WSL 自身 PTY 通道时才是 F0。**索引中「WSL = F0」的简化写法应予修正**。
  - **D2/D6**：索引应补指针以覆盖「标 P0 但非 OQ 行」的待决项；`OQ-PM-06` 应拆为「P0 定窗口 / P1 定 rig」。
- **补充（内核终检回报后）**：
  1. **网格对齐的归属**：**测量契约**（RM-C golden 像素测量）归 kernel/06；**门禁判定载体**归 tools/design-gates（G3 / RP-05）。两处表述以本条为准。
  2. **L-15（【新增】指标跨分册总数）按「设计上不做」关闭**：总数是派生产物且反复漂移，改为**各分册 §8 自持**，索引不得声称跨分册总数。
  3. **L-12（spec 07 历史变更记录仍写 S1–S9/B1–B8）按「不修改历史」纪律关闭**，不再作为缺口登记。
- **代价**：A 档裁决提高了 P0 的语料与流程成本（≥2000 用例、规则豁免期管理、权重冻结流程）；换取的是门禁数字可审计。
- **反方保留**：对 A1 有观点认为 ≥2000 用例投入过大，真实语料回放已足够。**否决理由**：只靠真实语料会导致差异表膨胀、回到「跑不动就绿灯」。**复议条件**：若连续两个 minor 的差异表稳定在预算内且无新增未登记差异，可复评用例基数。

---

## 3. 决策登记（DC）

> 由各角色 D 条目合并去重而来；每条必须可验收（对应 §8）。「来源」列指向角色编号。

### 3.1 产品与体验

| 编号 | 决策 | 来源 |
| --- | --- | --- |
| DC-01 | AI 一等公民，但聊天框是兜底层级；主交互是 inline + 命令级 + 可 diff/审批/回滚 | 01 |
| DC-02 | AI 交互入口顺序固定（内联 → 面板 → ghost text → 全屏 chat 次级） | 02 |
| DC-03 | 信息架构 Workspace > Tab > Pane(≤8)；PTY Session / Agent Session / Workspace 三个「会话」禁止混称 | 02 |
| DC-04 | 恢复语义只还原 cwd/env/布局/滚动/历史，**绝不自动重放命令或重启进程** | 02 |
| DC-05 | 错误是一等公民 artifact；永不吞 stderr、永不美化退出码 | 02 |
| DC-06 | 冻结 anti-features（§10），写入产品宪法 | 01 |
| DC-07 | 北极星 = Accepted AI Actions per WAU；护栏指标见 §1.4 | 01/02 |
| DC-08 | 命令面板 = Cmd+K，功能覆盖率 100% 为 CI 门禁 | 02/05 |

### 3.2 设计与视觉

| 编号 | 决策 | 来源 |
| --- | --- | --- |
| DC-09 | token 为唯一真相源（tokens/*.json → codegen TS/CSS/Rust + 主题 schema），CI 强制零硬编码色值 | 03/05 |
| DC-10 | ~~暗底高对比 + 单一冷青/靛蓝 accent；圆角 4/8/12；动效 120/180/240ms~~ **已被 AR-22 取代**（azure 蓝、浅色一等主题、标尺见 AR-22） | 03 |
| DC-11 | AI 输出用结构化视觉语法 + 四级风险色阶；destructive 强制二次确认 | 03/05 |
| DC-12 | 主题 = 纯数据（JSON + Schema + 版本 + 可选签名），禁止脚本/CSS 注入；永久免费开放 | 03 |
| DC-13 | 词法：对比度正文 ≥4.5:1（AA 门禁），官方主题默认目标 ≥7:1 且 CI 校验 | 02/03 |
| DC-14 | 视觉回归为 CI 门禁（diff ≤0.1%），非白名单区域零变化 | 03/05 |

### 3.3 内核与架构

| 编号 | 决策 | 来源 |
| --- | --- | --- |
| DC-15 | 核心 100% Rust；TS 仅存在于 Web 面板与插件 SDK | 10 |
| DC-16 | PTY 抽象 PtyBackend；Windows 唯一生产路径 ConPTY（Job Object 管理进程树，Win10 1809 为地板） | 04/10 |
| DC-17 | wgpu + rustybuzz/swash，多页字形 atlas，grapheme cluster shaping 再映射列 | 04/05 |
| DC-18 | sessiond 持久会话守护进程为唯一真源，可多端 attach | 04 |
| DC-19 | 原生 SSH（russh）；容器/WSL 统一抽象为 Transport，不 shell out 系统 ssh | 04 |
| DC-20 | 滚动缓冲 line-arena + style-run，默认 10k 行/10MB 上限，冷行下沉磁盘 | 04 |
| DC-21 | 仓库 Cargo + pnpm workspace，依赖单向无环（core ← session ← {agent, plugin-host}），CI 强制 | 10 |
| DC-22 | 通信 termai-ipc（二进制 + 版本/能力协商）；JSON-RPC 仅调试与第三方入口 | 10 |
| DC-23 | 存储 Session Log（唯一真相）+ SQLite 派生索引（可重建）+ CAS；格式版本 + 迁移器兼容 ≥2 大版本 | 10 |
| DC-24 | Local API = JSON-RPC over UDS/named pipe（含 apiVersion），同时服务插件、CLI --json、headless、CI、IDE 集成；**只维护一套契约** | 09 |
| DC-25 | TermAI 自绘 surface 上的受限控件集 ≤20 个；绝不自研通用 UI 工具链 | 10 |

### 3.4 AI 与安全

| 编号 | 决策 | 来源 |
| --- | --- | --- |
| DC-26 | 工具 ABI：name/json_schema/risk/idempotent/timeout/approval_policy/render_hint；输出统一 envelope | 07 |
| DC-27 | shell 执行在独立 watchdog 进程 + shell AST 预分析；解析失败按更高风险保守处理 | 07 |
| DC-28 | 工具层自研 ABI + 兼容 MCP；MCP 独立进程沙箱 + Policy Engine 二次校验 | 07 |
| DC-29 | 混合模型路由（本地分类 P95<50ms / 云端诊断首 token<3s）；BYO-key 一等公民；本地嵌入式向量库；离线只做规则解释，不做诊断幻觉 | 07 |
| DC-30 | 记忆三层（会话/项目 .termai//全局），默认本地可见可删；**项目记忆默认不提交 git** | 07 |
| DC-31 | 评测先行：AI 合入必须带离线 eval；「危险命令率上升」即阻断合入 | 07 |
| DC-32 | 七条命令黄金规则强制且不可关闭（§6.2） | 08 |
| DC-33 | 脱敏在上下文构建器内完成（正则 + 熵检测 + keychain 反向匹配）；脱敏失败默认**拒绝发送**并告知 | 08 |
| DC-34 | 审计 append-only + 本地哈希链，可导出 SIEM；记录操作者（人/插件/模型）、审批人、结果 | 08 |
| DC-35 | capability-based 默认拒绝；企业策略可加强不可削弱 | 08 |
| DC-36 | 模型视为供应链：权重哈希 + 分发通道签名 + 离线校验 | 08 |
| DC-37 | VT/PTY/IPC/插件消息持续 fuzz（≥10⁸ 次无崩溃），崩溃全部回归 | 08/10 |

### 3.5 平台与生态

| 编号 | 决策 | 来源 |
| --- | --- | --- |
| DC-38 | 插件宿主独立进程 + 每插件独立 wasm store + watchdog，3 次崩溃进 safe mode；宿主**按需启动**，空载预算 ≤80MB（不占用核心 120MB 基线） | 09/10 |
| DC-39 | 扩展点三级：Tier1 冻结开放 / Tier2 试验 / Tier3 永禁（帧内绘制、直改 VT、直写输出流） | 09 |
| DC-40 | 兼容 N-2 minor + 6 个月 deprecation + codemod；破坏性 WIT 变更需 RFC + 2 maintainer | 09 |

---

## 4. 目标架构

### 4.1 进程与组件拓扑

    ┌──────────────────────────────────────────────────────────────┐
    │ TermAI Desktop (UI Process)                                  │
    │  ├─ L0/L1/L3 Native Surface  (Rust + wgpu: 网格/外壳/动画)     │
    │  └─ L2 Web Shell (系统 WebView: AI 面板/设置/插件视图)         │
    │  └─ L4 Platform Native (IME 候选窗/右键菜单/通知, 最高 z)      │
    └───────────────▲───────────────────────────┬──────────────────┘
                    │ termai-ipc (二进制/版本握手) │
    ┌───────────────┴───────────────────────────▼──────────────────┐
    │ sessiond  (会话唯一真源, 7×24, 可多端 attach)                  │
    │  PTY/ConPTY · VT · Grid · Scrollback · Shell Integration      │
    │  Transport: Local | SSH | Container | WSL                     │
    │  ── 产出 ──▶ Session Log (append-only) + Context Events       │
    └───────▲───────────────────────┬──────────────────────────────┘
            │ 只读订阅 / 受控写入     │ 结构化事件
    ┌───────┴───────────┐   ┌───────▼────────────┐   ┌──────────────┐
    │ termai-agent      │   │ SQLite 派生索引     │   │ plugin-host  │
    │ (独立进程+watchdog)│   │ (可重建) + CAS      │   │ (独立进程)    │
    │ 单 Agent 主循环    │   └────────────────────┘   │ WASM 组件     │
    │ Model Router      │   ┌────────────────────┐   │ trusted subproc│
    │ Context Builder   │   │ Local API (JSON-RPC)│◀──│ capability    │
    │ Policy Engine     │◀──│ UDS/named pipe      │   └──────────────┘
    └───────┬───────────┘   └────────────────────┘
            │ 仅出站、可审计、默认 BYO-key 直连
            ▼
    ┌──────────────────────────────────────────────────────────────┐
    │ Cloud Control Plane (可选, 控制面 only)                        │
    │ Auth/OIDC · Entitlement · Sync(CRDT) · AI Gateway(路由计量)   │
    │ Plugin Registry(静态 CDN+签名) · Telemetry(opt-in) · Billing   │
    │ ✗ 不持有 PTY 流 / 文件 / 输出 / prompt / 密钥                   │
    └──────────────────────────────────────────────────────────────┘

### 4.2 渲染分层与合成

| 层 | 归属 | 内容 | 规则 |
| --- | --- | --- | --- |
| L0 网格 | Rust/wgpu | 字形、SGR、光标、选区 | damage 增量重绘；**禁止走 Web** |
| L1 外壳 | Rust/wgpu | tab、分隔条、状态栏、命令面板 | 同一 render target 二次绘制 |
| L2 面板 | 系统 WebView | AI 对话、设置、diff、插件视图 | 独立 OS 子表面；Core 定位裁剪；崩溃不影响会话 |
| L3 动画 | Rust | 分屏插值、面板滑入、光标闪烁 | 统一时钟；WebView 不得驱动关键动画 |
| L4 浮层/IME | 平台原生 | 右键菜单、tooltip、候选窗 | 最高 z-order，L2 不得覆盖 |

### 4.3 模块与依赖方向（单向，CI 强制）

    tokens(叶子) ── core-dto(IDL 生成,叶子)
       │                    │
       ▼                    ▼
    term-render(→vt,gpu) → ui-native → shell-bridge → web-shell → plugin-ui-sdk
                                     │
    core ← session ← { agent , plugin-host }
    apps/* 不得被任何库依赖

### 4.4 硬边界速查

- **允许 AI 做的**：只读订阅结构化上下文、生成结构化意图（tool + args）、请求审批、写审计。
- **禁止 AI 做的**：产出可执行字符串直接交给 shell、绕过审批、自行提权、读原始 secret、抓屏获取上下文。
- **允许插件做的**：Tier1 声明式贡献点、只读 scrollback 镜像、注册 AI 工具（默认需审批）。
- **禁止插件做的**：写 PTY/输出流、帧内任意绘制、持有 Node/原生句柄、静默网络访问。

---

## 5. 统一非功能性预算

> 口径仲裁见 AR-19：**门禁 = 达不到即阻塞发布**；**目标 = 努力的挑战值**。

| 指标 | 发布门禁 | 目标 | 测量 |
| --- | --- | --- | --- |
| 冷启动到可输入 | P95 ≤150ms | ≤100ms | release 构建 CI 脚本，SSD |
| key-to-photon（本地） | P99 ≤16ms | P99 ≤8ms（P50 ≤4ms） | 时序探针 + 高速相机回归 |
| PTY 层附加延迟（子指标，AR-30） | P99 ≤2ms | — | kernel/06 |
| 4K @120Hz 网格帧时 | <8.3ms（丢帧 <0.1%） | — | 合成基准 |
| 解析+渲染吞吐 | ≥500MB/s | — | 1GB 语料基准 |
| 空闲 RSS（1 万行）**核心进程组 = sessiond + 原生 UI，不含 WebView 与插件宿主** | ≤120MB | ≤100MB（无 WebView 时） | 进程采样（方法见 kernel/06） |
| AI 面板（WebView）就绪后的 RSS 增量（**独立记账**，不并入 120MB） | ≤60MB | — | kernel/06 |
| 空闲 CPU（窗口聚焦、无输出） | ≤1%（RM-A，60s 均值） | — | kernel/06 |
| 未聚焦 / 被遮挡时的出帧数 | = 0 帧/10s | — | 结构断言（kernel/03、kernel/06） |
| 24h RSS 斜率 | <1MB/h | — | 长跑 |
| 插件宿主空载 | ≤80MB（仅启用插件时计入） | — | 独立进程采样 |
| 输入字节等价（键盘 / 粘贴 / IME commit 全语料回放，逐字节比对） | = 100% | — | kernel/05 IN-AC 语料 |
| 安装包 | <60MB | — | 产物度量 |
| AI 网关附加延迟 | p95 <120ms / p99 <300ms | — | 服务端 SLO |
| 诊断首 token | P95 <3s | — | 在线埋点 |
| 控制面单 MAU 成本 | ≤ $0.05/月 | — | 成本看板 |
| 视觉回归 diff | ≤0.1% | 0 | Playwright 像素 diff |
| 硬编码色值 | = 0 | — | CI 静态检查 |
| 网格对齐误差 | ≤0.5px | — | 渲染用例 |

> **测量方法归属（AR-24 第 3 条）**：本表每一行的**测量实现、采样次数、统计口径与不确定判据**，以 `docs/spec/kernel/06-performance-methodology.md` 为**唯一权威**；行内「测量」列只是摘要，不构成实现依据。
> **语料归属**：语料可由专项分册提供（例如「输入字节等价」的语料在 kernel/05、「VT 兼容」的语料在 kernel/01），但**方法与判据仍须在 kernel/06 落地**。
> **映射要求**：kernel/06 必须维护一张「§5 指标 → 测量定义」映射表；任何新增或修改的 §5 指标若未在该表登记，不得进入本表。

---

## 6. 安全与信任黄金规则

### 6.1 三条红线（不可协商）

1. **无人类显式批准，AI 不得执行破坏性/外发型/提权/跨主机操作。**
2. **secret 永不进入模型上下文、日志与遥测**；脱敏在上下文构建器完成，不依赖模型自律。
3. **默认本地优先**：scrollback/历史/文件默认不出设备；上传须 opt-in、可撤销、可审计。

### 6.2 AI 执行命令七条黄金规则（强制、不可关闭）

| # | 规则 |
| --- | --- |
| 1 | **能力与意图分离**：模型只产出结构化意图，命令由确定性执行器构造 |
| 2 | **禁止 shell -c 拼接**：统一 argv 数组 + 显式转义 |
| 3 | **默认 dry-run**：先展示命令、影响面（读/写/网络/提权）与目标主机/路径 |
| 4 | **危险动作逐条批准**：破坏性/外发型/提权/跨主机不得整类「永远允许」 |
| 5 | **污点追踪**：不可信来源的值不得直接成为命令参数或路径，须转义并标注来源 |
| 6 | **最小权限执行**：AI 子进程默认无网络、只读 FS 白名单、不转发 SSH agent，提权到期回收 |
| 7 | **可回滚**：写操作前置快照，支持「撤销上次 AI 操作」（限工作区文件与 git 可逆操作） |

### 6.3 数据分级

| 等级 | 数据 | 默认处理 |
| --- | --- | --- |
| L0 公开 | 版本、帮助文本 | 可上传 |
| L1 内部 | 命令名、错误码（脱敏后） | opt-in 遥测 |
| L2 敏感 | 路径、源码、主机名、业务日志 | 默认本地，不出设备 |
| L3 机密 | 凭据、私钥、token、客户数据 | 从不入 prompt/日志/遥测 |

---

## 7. 路线图（按依赖而非日历）

| Phase | 主题 | 交付定义（Exit Criteria） |
| --- | --- | --- |
| **P0 信任基座** | PTY/VT/GPU 渲染/跨平台 | vttest + esctest 全通过；三平台 IME/CJK 矩阵全绿；性能门禁进 CI；screen 可恢复 |
| **P1 结构化上下文** | Session Log、OSC 133/633、Local API、headless | 所有会话状态可经 API 读取；SQLite 索引可从 Log 完整重建；termai daemon 可跑 CI |
| **P2 AI 一等公民** | 内联 Agent、审批、审计、回滚、eval | 100% AI 动作可审计；L2/L3 未审批执行 = 0；TermBench 与危险命令率门禁生效 |
| **P3 生态** | WASM 插件、MCP、兼容层、命令面板 100% | 第三方可零特权发布插件；TTFHW ≤5min；签名校验失败 100% 拒装 |
| **P4 团队/企业** | 同步、策略、SSO/SCIM、私有部署 | 无外网环境功能完整；越权用例 0 通过；审计可导出 SIEM |
| **P5 远程/Web/协作** | 远程会话、Web 客户端、协作共享 | 端到端加密；权限模型明确「谁能写 stdin / 谁能读 AI 上下文」 |

**阶段门禁**：P2 末复核「WebView 是否进最低依赖」「componentize-js DX 指标」（TTFHW ≤5min 且热重载 p95 ≤300ms；未达标则启用每插件 V8 isolate 第二运行时）。

---

## 8. 质量门禁与验收

### 8.1 CI 六件套（缺一不得合并）

1. **VT 兼容**：vttest / esctest / kitty 套件 100%；xterm 兼容用例 ≥99%（差异登记在案）。
2. **行为回放**：录制真实会话字节流 + 输入脚本，断言最终网格与状态（通过率 ≥99.5%）。
3. **视觉回归**：像素 diff ≤0.1%，非白名单区域零变化。
4. **性能门禁**：§5 全部门禁项；回归 >5% 阻断合并。
5. **依赖与许可**：cargo-deny + cargo-audit + npm audit + CycloneDX SBOM；链接边界 GPL/AGPL 依赖 = 0。
6. **安全与 fuzz**：VT/PTY/IPC/插件消息 fuzz 24h 无 crash；插件逃逸用例 0 成功。

### 8.2 关键验收（按主题）

| 主题 | 验收标准 |
| --- | --- |
| 可靠性 | sessiond 7×24 无泄漏（RSS 漂移 <5%）；UI 崩溃 <2s 重连且屏幕一致；**sessiond 重建 P95 ≤2s / P99 ≤5s（AR-26 第 4 条）**；**孤儿进程清理 = 100%（会话关闭 ≤2s 回收，AR-30）**；crash-free session >99.9% |
| AI 安全 | L2/L3 未审批执行 = 0；注入用例拦截 ≥95%；出网密钥命中 = 0；100% 出网可在 UI 回看 |
| 隐私 | 默认配置抓包零用户未发起连接；遥测关闭时上传 0 字节；服务端 prompt/completion 字段 = 0 |
| 可访问性 | 核心任务键盘完成率 100%；NVDA/VoiceOver 完成 UI 层 6 条核心流程；reduced-motion 下动画 = 0 |
| 可用性 | TTFC ≤3 分钟且零弹窗；危险命令误执行率 <0.5%；盲确认率 <15% |
| 生态 | TTFHW ≤5min（成功率 ≥95%）；热重载 p95 ≤300ms；崩溃隔离 100%；1.0 时 ≥50 个过审插件 |
| 发布 | 任一 stable 版本 ≤15 分钟全量回滚；nightly 连续 30 天不中断 |

---

## 9. 风险登记册（Top 12）

| # | 风险 | 触发条件 | 缓解 | 责任角色 |
| --- | --- | --- | --- | --- |
| R1 | 自研受限控件集拖垮 UI 进度 | 控件需求 >20 个或出现主题化诉求 | 硬约束控件集；复杂界面一律 WebView | 05/10 |
| R2 | 三平台 IME 与原生/Web seam 错位 | CJK 用户输入异常或候选窗漂移 | 原型先行；候选窗全原生；CJK 回归矩阵进发版门禁 | 05/02 |
| R3 | VT 兼容长尾 | vttest/回放通过率 <99% | 公开测试集 + 真实语料回放 + 差异登记 | 04 |
| R4 | ConPTY 行为黑洞 | Windows 上 TUI 表现异常 | 兼容矩阵（vim/htop/neovim/fzf/tmux 嵌套）+ NativePty 插槽 | 04 |
| R5 | Prompt injection 经终端输出劫持 AI | 模型按不可信内容执行命令 | 数据/指令分离 + 不可信信封 + Policy Engine 二次校验 + 注入回归集 | 07/08 |
| R6 | 审批疲劳导致盲确认 | 5 秒内直接确认比例 ≥15% | 分级摩擦 + 白名单 + 可解释提示 | 02/08 |
| R7 | 性能预算渐进侵蚀 | 冷启动 >150ms 或延迟 P99 >16ms 连续两版 | 性能门禁进 CI，回归即阻塞 | 10 |
| R8 | Linux WebView 碎片化 | ≥2 发行版插件 UI 渲染异常 | WebView 可选依赖 + 纯原生降级面板 | 10/05 |
| R9 | 插件供应链投毒 | 签名异常或恶意插件报告 | 签名 + 透明度日志 + 能力最小化 + 远程吊销 ≤1h | 09/08 |
| R10 | WASM DX 劝退作者 | TTFHW >5min 或热重载 >300ms | AOT 预编译 + 实例池 + 组件 ≤3MB 预算 + V8 isolate fallback 门 | 09 |
| R11 | 云边界被功能需求侵蚀 | 出现「上传历史换同步」类需求 | 控制面-only 写入不可协商清单；CI 静态禁止 prompt 字段 | 06/08 |
| R12 | 许可策略摇摆 | 核心许可 12 个月内变更 | 章程：变更需 TSC 2/3 + 90 天公示 | 10 |

---

## 10. Anti-features（项目宪法级「明确不做」）

1. 自有 shell 语言 / 自研 shell。
2. 内置编辑器 / IDE。
3. AI 全自动免审批执行（含「信任模式」放行 L2 及以上）。
4. 终端内运行完整 Web 应用。
5. 云端 PTY / 全云终端。
6. 默认上传命令历史、scrollback 或任何终端内容。
7. 服务端保存 prompt/completion 用于训练。
8. 前端持有用户 API Key 明文或承担密钥中转。
9. 第三方原生动态库插件。
10. 帧内任意绘制 / 插件直写 PTY 与输出流。
11. 内嵌 Lua/JS 作为**配置**语言（可作为插件作者语言复议）。
12. 零基础教学模式、课程式引导、开机欢迎向导。
13. 逐条弹窗式确认（改为分级摩擦）与「不可回滚却暗示可回滚」的措辞。
14. GPU subpixel AA 承诺。
15. 自建账号体系（采用 OIDC/设备码）。

---

## 11. 待决议问题（Open Questions）

> 合并 10 份文档的 OQ 并按影响排序。每条给出**建议**与**必须决策的阶段**。

| # | 问题 | 建议 | 决策阶段 |
| --- | --- | --- | --- |
| OQ-01 | ~~核心最终许可 Apache-2.0 还是 MIT~~ | **已裁决（发起人）：Apache-2.0 OR MIT 双许可 + 全栈开源**，见 AR-21 / ADR-0013 | 已关闭 |
| OQ-02 | WebView 是否进入最低支持依赖 | 否，缺失降级为原生文本面板 | P0 |
| OQ-03 | WebView 版本策略 evergreen vs fixed | 默认 evergreen，企业提供 fixed | P1 |
| OQ-04 | 持久 scrollback 默认：内存即清 vs 加密持久 | 元数据默认持久，原始输出默认不持久 | P1 |
| OQ-05 | sessiond 进程模型：每用户单实例 vs 每会话一进程 | 每用户单守护 + 每会话子任务（**ADR-0009 已预先采纳，P1 复核**） | P1 |
| OQ-06 | 是否默认开启 kitty keyboard protocol | 否，按应用探测后启用 | P1 |
| OQ-07 | 本地小模型形态（内置 vs 按需下载） | 按需下载 + 可选内置精简模型 | P2 |
| OQ-08 | 「信任模式」是否可自动放行 L1 | 可，但默认关闭且记录审计 | P2 |
| OQ-09 | MCP server 默认信任级别 | 默认拒绝，显式批准后只读 | P2 |
| OQ-10 | 评测真值来源（会话捐赠 vs 合成集） | 合成集为主，捐赠 opt-in 为辅 | P2 |
| OQ-11 | Tier2 插件 iframe UI 首版是否开放 | 否，红队通过后开放 | P3 |
| OQ-12 | side-load 自签名插件是否允许 | 允许，永久标记 Untrusted 并禁用 subprocess/secrets | P3 |
| OQ-13 | .termai/ 项目记忆是否默认提交 git | 否，提供显式导出 | P2 |
| OQ-14 | 插件市场长期是否抽成 | v1 零抽成；企业 registry 收费 | P3 |
| OQ-15 | 数据驻留粒度（租户级 vs 文档级） | 租户级 | P4 |
| OQ-16 | 协作传输：P2P/WebRTC vs 服务端中继 | 默认 P2P + 中继兜底 | P5 |
| OQ-17 | 是否提供自托管控制面 | 企业版提供，SLA 单独定义 | P4 |
| OQ-18 | 远程/Web 终端是否为一等能力（影响 IPC 加密与鉴权边界） | 是，但 P5 再做，需提前在 IPC 预留 | P1 规划 |
| OQ-19 | 是否成立独立 TSC 与治理章程 | 成立，3 年内 BDFL + 公开 ADR 过渡 | P0 |
| OQ-20 | 撤销栈是否覆盖容器/远端文件系统 | 不覆盖，L2 明示「部分可撤销」 | P2 |

### 11.1 规格阶段新增待决议（由 docs/spec 汇聚，按影响面排序）

| # | 问题 | 建议 | 决策阶段（来源） |
| --- | --- | --- | --- |
| OQ-21 | NSM 计量口径：遥测默认关闭下「Accepted AI Action」如何判定（显式确认 vs 60s 未撤销；解释类是否计入）？WAU 分母在禁止自建账号体系下如何定义？ | 显式确认 + 未撤销双条件；解释类不计入；WAU = 本地设备匿名计数（用户可查可清）。**在定稿前 NSM 不得作为对外承诺。** | P2（01-spec OQ-P-01/02/03） |
| OQ-22 | 授权档 session 的作用域粒度（PTY Session / Agent Session / Workspace） | 默认 Agent Session 级，且不跨 Workspace | P2（02-spec UX-OQ-2） |
| OQ-23 | AI 解释/诊断的默认落点（侧栏 vs 内联） | 诊断/解释默认侧栏；命令与 diff 默认内联，>40 行强制折叠 | P2（02-spec UX-OQ-3） |
| OQ-24 | 子 Agent 的 capability 裁剪、并发上限与 token 预算归属 | 只读工具、无审批权、并发 ≤4、计入父预算 | P2（04-spec OQ-AI-02） |
| OQ-25 | Tool ABI 是否同享 N-2 minor 兼容承诺（DC-40 原文仅覆盖插件 WIT） | 同口径：N-2 minor + 6 个月 deprecation | P2（04-spec OQ-AI-01） |
| OQ-26 | ~~参考机 SKU；4 shell × 3 字体后端枚举；arm64 支持级别~~ | **已裁决（ADR-0014）**：三档自托管参考机（RM-A/B/C，云 runner NON-GATING）；12 组合全 v1 必修；v1 门禁架构 = Win x64 / Linux x64 / macOS arm64，Linux arm64 支持但 P2 升门禁，**Windows arm64 非 v1、P2 进入** | 已关闭 |
| OQ-27 | ~~「链接边界」可执行定义；弱 copyleft 准入~~ | **已裁决（ADR-0015）**：边界 = 与第一方代码同进程执行或被并入/派生的第三方单元（P1 同进程即入界 / P2 派生即继承 / P3 未知即拒绝）；判定表 LB-01…LB-18（A/R/D 三档）；**MPL-2.0 允许（禁内联）**，LGPL 仅限未修改的动态链接，EPL/CDDL 逐案审批；subprocess 调 GPL 工具允许、调 AGPL 视方向审批 | 已关闭 |
| OQ-28 | ~~审计保留期/外锚；崩溃端点归属；签名密钥门限与 SLSA；nightly 中断口径~~ | **已裁决（ADR-0014 + ADR-0015）**：审计默认 90 天（可配 7–3650，红线 ≥30）、外锚默认企业自持不经我方云；崩溃端点自托管，原始 30 天/聚合 90 天硬删除；离线根 2-of-3（HSM + YubiKey）+ 子键 2-of-3、仅签哈希；**P0–P2 强制 SLSA Build L2 地板、P3 前达 L3、不追 L4**；nightly 分 FAIL-REPO（计入）/ EXTERNAL（双人签署 + 证据 + 不投递，不计入） | 已关闭 |
| OQ-29 | 远程/Web 形态下插件执行位置；气隙与离线吊销语义；插件 AI tool 的 destructive 分类裁决者；插件 SPDX 白名单（含 subprocess 调 GPL 可执行文件） | 插件本地执行；吊销以签名版本清单离线校验；分类由 Tool ABI risk 字段 + 安全评审；subprocess 调 GPL 工具需法务裁定 | P3（06-spec OQ-06-01/04/06/07） |
| OQ-30 | 多端 attach 的 stdin 单写者协议；Local API 鉴权信任边界；IPC 背压与最大帧；索引重建一致性 | 单写者租约 + 只读多订阅；Local API 仅本机凭证；帧上限 8MB + 背压丢弃策略；重建后哈希校验一致 | P1（03-spec OQ-A1/A2/A4） |
| OQ-31 | ~~默认主题：深色还是跟随系统~~ | **已决议（AR-23 第 7 条）：默认跟随系统**，系统不可判定时回退深色；两套主题均进 P0 门禁 | 已关闭 |
| OQ-32 | 图标集来源：自绘 SVG 还是采用开源图标集？后者必须过 ADR-0015 的许可白名单 | 允许引入开源图标集，但须通过 ADR-0015 的 SPDX 白名单与链接边界判定；自绘作为兜底 | P0（02-spec UX-OQ-14） |
| OQ-33 | 软换行开关的默认值与持久化位置（全局配置 / 每会话覆盖） | 默认开启；全局配置项 + 每会话可覆盖，随 Workspace 持久化 | P1（02-spec UX-OQ-15） |
| OQ-34 | 是否需要「紧凑模式」（AR-22 反方复议条件要求：若 dogfood 显示同任务耗时或会话数下降则提供） | 预留为 P3 的可选项，不进入 P0–P2；触发条件按 AR-22 的复议条件执行 | P3（02-spec UX-OQ-16 / AR-22） |
| OQ-35（内核系列指针） | 内核七分册的 77 条 OQ **不在本表逐条复制**（避免派生登记漂移）；权威登记在 docs/spec/kernel/00-index.md §5，三档处置与默认值在 docs/spec/kernel/00-p0-triage.md | 截至 AR-31：已决 25、部分已决 1、仍开放 52；其中 **P0 开放仅 8 条且全部为 B 档「已采纳默认值」，不阻塞开码**；C 档 3 条已降 P1 | 见各分册 §8 |

### 11.2 跨文档裁决与一致性修正（Orchestrator）

| # | 事项 | 裁决 | 影响文档 |
| --- | --- | --- | --- |
| CR-01 | 风险分级命名冲突：术语表原用 R0–R3，AR-06 用 L0–L3/U，且术语表自称「冲突以术语表为准」 | 唯一枚举 = **L0/L1/L2/L3/U**；效力层级确立为 **HARNESS > 术语表 > 角色原文**。关闭 02-spec UX-OQ-1 与 04-spec OQ-AI-10 | 00-glossary、02-spec、04-spec、05-spec |
| CR-02 | 05-spec 的脱敏管线阶段 R0–R6 与已废弃的风险分级 R 编号碰撞 | 脱敏管线统一改名 **RED-0…RED-6**，并在术语表登记，禁止与风险分级、§9 风险登记册 R1–R12 混用 | 05-spec、00-glossary |
| CR-03 | ADR 文件命名（ADR-NNNN-slug）与术语表约定（NNNN-slug）不一致 | 统一为 **docs/adr/ADR-NNNN-<slug>.md**（ADR README §7 已声明） | docs/adr/、00-glossary |
| CR-04 | OQ-05 sessiond 进程模型在 ADR-0009 被提前固定 | 认可 ADR-0009 的「每用户单守护 + 每会话子任务」，在 §11 OQ-05 标注为**已预先采纳、P1 复核** | ADR-0009、HARNESS §11 |
| CR-05 | 遥测默认关闭（AR-12）与 NSM 可测量性冲突 | 指标以本地匿名计数为主；NSM 判定口径 P2 定稿，定稿前不得对外承诺（见 OQ-21） | 01-spec、HARNESS §1.4 |
| CR-06 | 发起人裁决：TermAI 为**开源项目**，范围全栈开源 | 许可统一 **Apache-2.0 OR MIT 双许可**；取消「云端闭源模块」；AR-10 被 **AR-21** 取代；新增 **ADR-0013**；四条不可变商业承诺保留。已同步 01/02/03/05/06/07-spec 与 ADR 索引；ADR-0006 内的 AR-10 引用按治理规则作冻结历史保留，其「市场零抽成」承诺已由 AR-21 §4 承接 | AR-21、ADR-0013、01/03/06-spec、ADR README |
| CR-07 | 发起人确认：WebView 为**可选依赖**（原回答「是」含义澄清） | 维持 AR-02：缺失时降级为纯原生文本面板；OQ-02 关闭 | AR-02、03-spec、06-spec |
| CR-11 | P0 内核七份评审到齐，暴露跨文档冲突（RSS 口径、CPU/功耗无门禁、Log 保留语义、PR/REL 双口径、OQ 撞号、快捷键优先级） | 一次性裁决为 **AR-24…AR-29**（度量口径 / 字节保真与车道 / 三层保留语义 / 性能双口径 / 契约变更与命名空间 / 输入与剪贴板默认），并派单修跨文档不一致 | AR-24…AR-29、docs/spec/kernel/** |
| CR-10 | 发起人要求「终端为主体、agent/工作区统一进侧栏、设置独立窗口、新建终端即标签页选择」，并更正窗口控制为「右上角、跟随平台」 | **ADR-0017 转 Accepted**，落地为 **AR-23**；修订 AR-16 与 AR-22 第 7 条；软换行关闭语义定为**视觉裁剪**；默认主题定为**跟随系统**（关闭 OQ-31）；窗口控制取方案 A（位置随平台、外观统一） | ADR-0017、AR-23、AR-16、AR-22 §7、docs/spec/02 |
| CR-09 | 发起人否决 v1 视觉、确认采用 Netcatty 取向并追加六条要求 | **ADR-0016 转 Accepted**，落地为 **AR-22**（取代 DC-10）；docs/spec/02 按 AR-22 重写；跨会话 Agent 写操作 **P3** 起、P2 只做只读；`prototype/reference/` 第三方截图**排除出版本库**（已加 .gitignore） | ADR-0016、AR-22、HARNESS §3 DC-10、docs/spec/02、.gitignore |
| CR-12 | M0 交付期发现五处规格表述不可按其字面实现（repr(C) 线格式与 24 字节契约冲突、Hello 缺 required 承载字段、SessionId 宽度不一致、trait 签名非法） | 立 **ADR-0020** 作实现期 errata：D1 线格式以显式小端为唯一权威（禁止 repr(C) 直转）；D2 Hello 增 required 并回传 unknown_optional；D3 SessionId 统一 u128 ULID；D4 叶子契约用 &mut dyn 且不加 Send；D5 分册正文另行修订，本 ADR 不改设计结论。证据见 docs/plan/m0-spec-defects.md（SD-01…SD-05） | ADR-0020、kernel/07 §3.1/§3.3、kernel/04 §3.2.2/§4、kernel/05 §3.1、HARNESS §11.2 |
| CR-13 | CI 门禁编号口径不一致：HARNESS §8.1 与 AGENTS §4 称「六件套」（不编号），spec 07 §3.4.1 编为 G1–G6 后又把 G7（设计静态）/G8（设计浏览器）列为合并阻断（实为 8 项）；ci.yml 头注释与步骤名写「S1-S9 / B1-B9」，而 tools/design-gates 实际执行 S1–S10（kernel/00-index §4 亦写 S1–S10 / B1–B10） | **不改任何门禁语义**，只统一表述：设计静态门禁一律写 **S1–S10**、浏览器门禁写 **B1–B10**；G1–G8 是 spec 07 的编号空间，HARNESS/AGENTS 的「六件套」指 §8.1 的 6 条主题（不等于 6 个 G 编号），spec 07 §3.4.1 的 A2 行需补注；ci.yml 注释已就地更正。登记为 SD-06 | HARNESS §8.1、AGENTS §4、spec 07 §3.4.1/§3.4.2、.github/workflows/ci.yml |
| CR-08 | P0 工程/合规议题（OQ-26/27/28）委托资深角色裁定 | **ADR-0014** 定平台矩阵与参考机（12 组合全 v1 必修、arm64 Windows 非 v1、GPU 四级降级、nightly 双口径、CI 月上限 $3,000）；**ADR-0015** 定链接边界（LB-01…LB-18 判定表、SPDX 白/黑名单、弱 copyleft、签名门限、SLSA L2→L3、审计与崩溃保留） | ADR-0014、ADR-0015、05-spec、07-spec |
| CR-16 | spec 07 §3.8.2 的扁平 legacy 对象与本仓库的测量口径权威 kernel/06 §3.7 的结构化字段同名冲突（commit / toolchain / runner 在 §3.7 是对象） | **不改 schema**：以 kernel/06 §3.7 为唯一权威（AR-24.3）；9 个 legacy 字段按 §3.7 落在 metric 级 + 顶层 commit；spec 07 §3.8.2 的扁平形态作为加法字段 flatProjection 承载（§3.7 允许 1 minor 加法字段）。登记为 SD-09 | spec 07 §3.8.2、kernel/06 §3.7、docs/plan/p0-spec-defects.md、tools/bench |
| CR-15 | 组件标签与物理 crate 命名未映射：HARNESS §4.3 与 spec 03 §3.3 的依赖图使用层级角色标签（term-render / ui-native / shell-bridge / web-shell / plugin-ui-sdk / core-dto / tokens / core / session / agent / plugin-host），而 spec 07 §3.1.1/§3.1.2 与 ADR-0019 使用物理 crate 名（termai-*）；且 spec 03 §3.3 把 core-dto 与 termai-core 并列为两个叶子，仓库只实现了一个 | **不改任何依赖方向结论**，只消除命名歧义：① 物理 crate 名以 termai-* 为唯一真源（ADR-0019 D1 已按此登记，K4 允许边表按此校验）；② HARNESS §4.3 与 spec 03 §3.3 依赖图中的标签是**层级角色名**，不是 crate 名；③ **core-dto 当前由 termai-core 承载**（IDL/DTO 与叶子契约同体，ADR-0019 D1 + ADR-0023 D3），拆出独立生成 crate 需新 ADR，并同时满足 spec 03 §3.3 第 3 条的双端 Rust/TS 生成义务；④ 未建组件（ui-native / shell-bridge / web-shell / plugin-ui-sdk 等）在创建时必须按 spec 07 §3.1.2 登记 owner 并同步 K4 允许边与 K7 覆盖率 | HARNESS §4.3、spec 03 §3.3、spec 07 §3.1.1/§3.1.2、ADR-0019、ADR-0023 |
| CR-17 | P0 待决项 D-1…D-4 与 D-6 遗留的 H12/H13 经评审定案；其中 **D-1 的前提与 kernel/01 §5 冲突**（p0-open-decisions 把 esctest 与 xterm 语料当成同一套件） | 立 **ADR-0029**：D-1 **更正前提**——esctest = kernel/01 **V-02**，要求 `R_strict=1 且 X=0`；「≥99% + 差异登记」属 **V-04**（xterm 语料）；**否决「读法 B」**（对 esctest 登记差异会要求 `X>0`，与 K-01 冲突并构成 §8.1-1 放宽 → 需 TSC，当前无合法批准人）。D-2 `OSC 4/10/11/12` 明文声明 v1 不实现、不应答（AR-20），能力缺失经 `suites.toml` 静态能力前置 `S_cap` 排除。D-3 `DA/DA2/DECID` 必须实现且与钉定 xterm oracle 逐字节一致（声称集 ⊆ 实现集；`TERM_PROGRAM=TermAI` 如实自报）。D-4 §8.2 可靠性时序测量归 **kernel/06 §3.10** + `tools/bench` 独立 `RELIABILITY_MAPPING`（**不得**改 `SECTION5_MAPPING`）。H13/`B7`：`gatingNumbersProduced` 仅计**机器绑定**的门禁数字。H12 的 `governed: partial` = 方法归 kernel/06、语料与车道归 kernel/05 + G1/G2 | ADR-0029、kernel/01 §3.5、kernel/06 §3.9/§3.10、docs/plan/p0-open-decisions.md、tools/bench/registry.mjs |
| CR-18 | ADR-0029 D-3 的「与钉定 oracle 逐字节一致」在实施前被证明不可执行：esctest 的 DA 断言是**集合包含 + 范围**（`da.py` / `da2.py`），级别 5 的 expected 要求声称本实现没有的能力（selective erase / locator / color / rectangular editing 等），逐字节一致会同时违反 AR-20 与 D-3 自己的「声称集 ⊆ 实现集」 | 立 **ADR-0030**：**级别规则化**——声明的 VT 级别 = xterm DA1 在该级别的 expected 集合**全部为已实现能力**的最高级别（当前 = **1**，`expected=[1,2]`）；**禁止**按失败数选级别；esctest **必须显式** `--max-vt-level`，且 G1 报告必须**并列**级别与 eligible 分母（实测：level1 = 99 passed / 378 known-bug / **90 failed** / eligible 189（**第 261 轮勘误：D-3 后为 103/378/86；经 D-2 的 `color-query` 排除后 gate-eligible 142、failed_real 39，换算由 `tools/conformance/esctest-report.mjs` 完成**）；level5 = 267/41/**259**/526；known-bug 中的「级别不足未运行」须单列为 `excluded_by_vt_level`）。DA1 应答 `CSI ? 1;2 c`、DA2 `CSI > 0;314;0 c`（Pv 为本产品自报版本）、DECID（8-bit C1 `0x9A`）依 **OQ-VT-14** 仅非 UTF-8 模式识别。**更正 ADR-0029 D-3 的「逐字节一致」为「满足该级别断言」**（诚实义务保留）。 | ADR-0030、ADR-0029 D-3、kernel/01 OQ-VT-14、p0-verification-runbook §2 |
| CR-14 | P0 立项契约盘点发现三处「数值即契约」缺口：attach 族无 msg_type；字符串态上限两个数字并存；GridSnapshot 丢组合字符且字段集仅临时冻结 | 立 **ADR-0023** 作 P0 契约 errata：D1 新增 0x05xx 会话接入段并分配 AttachRequest/AttachAck/TailReplay/DetachNotice（0x0504–0x05FF 保留）；D2 冻结字符串态上限为 AR-31 已采纳值（OSC/SOS/PM 1 MiB、DCS/APC 16 MiB）；D3 依 kernel/03 §3.3 正式冻结 GridSnapshot/GridDelta 字段集（含 clusters: ClusterTable，golden/digest 必须覆盖）。证据见 docs/plan/m0-spec-defects.md（SD-07/SD-08.1/SD-08.2） | ADR-0023、kernel/07 §3.2、kernel/01 §8、kernel/03 §3.3、HARNESS §11.2 |

> **规则**：本节只登记「不涉及架构取舍、仅为消除文档冲突」的修正。任何改变 AR/DC 结论的事项，必须走 RFC → 新 ADR，并在对应 AR 条目上标注「被 ADR-xxxx 取代」（见 AGENTS.md §5）。

---

## 12. 文档地图与维护约定

| 路径 | 内容 | 状态 |
| --- | --- | --- |
| HARNESS.md | 本文件：唯一权威总纲与仲裁记录 | v1.0 |
| AGENTS.md | 面向 AI/协作者的仓库工作约定与阅读顺序 | v1.0 |
| README.md | 项目简介与导航 | v1.0 |
| docs/roles/01–10-*.md | 10 份角色评审原文（含全部反方意见与推理） | 冻结 |
| docs/spec/00-glossary.md | 术语基线（**效力：HARNESS > 术语表 > 角色原文**） | v1.1 |
| docs/spec/01–07-*.md | 领域规格：01 产品与指标 / 02 UX 与设计系统 / 03 系统架构 / 04 AI Agent 平台 / 05 安全隐私合规 / 06 插件生态与 DevEx / 07 工程质量与发布 | v1 |
| docs/spec/kernel/ | **P0 内核设计分册（评审产物）**：00 索引 / 01 VT 兼容性 / 02 PTY 与平台层 / 03 渲染管线 / 04 会话生命周期 / 05 输入·IME·剪贴板 / 06 性能测量方法学 / 07 内核 API·ABI | v1 |
| docs/adr/ | 架构决策记录：README（流程 + 索引）+ ADR-0001…ADR-0031（**ADR-0027 / ADR-0031 为 Proposed；ADR-0028 / ADR-0029 / ADR-0030 为 Accepted**） | 已建 |
| docs/plan/ | **交付计划与实现期登记**：mvp-delivery-plan（M0 范围与可追溯矩阵）、p0-delivery-plan（P0 出口拆解、团队组织、WBS、波次与可追溯矩阵）、m0-spec-defects（SD-01…SD-08 实现期规格缺陷）、**p0-spec-defects（SD-09…SD-24 实现期规格缺陷）**、m0-delivery-report（交付报告）、**p0-open-decisions（P0 五项待决/已决的决策简报：选项、代价、建议、依据与证据链）**、**p0-verification-runbook（可执行验证手册：八条门禁命令含四项 selftest、两个协议探针、分诊三件套、六步验收顺序；已逐节复核）** | M0 / P0 |
| docs/audit/ | **未闭合清单与技术债登记**：`debt-p0.md`（E-P0-1…E-P0-4 的未闭合项 + SD 系列 + 治理缺口，含 owner / 触发条件 / 依据编号；spec 07 §3.9 的 P0 阶段实现，发版后按版本另建 `debt-<ver>.md`） | P0 |

**维护约定**：
1. 本文只增不改语义：新决议以 AR-21… 追加；修改已有 AR 必须新开 ADR 并在 AR 条目上标注「被 ADR-xxxx 取代」。
2. 每条 AR / DC 变更后，必须在 24 小时内同步到对应 docs/spec 与 CI 检查（若可自动化）。
3. 角色文档为**冻结证据**，不得改写；新的角色评审作为 docs/roles/ 的新编号追加。
