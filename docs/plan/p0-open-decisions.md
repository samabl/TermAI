# P0 待决策项（决策简报）——**D-1…D-6 均已决（D-1…D-4 由 ADR-0029，第 256 轮）**

> 用途：这五项**不是实现问题**，而是**判定域/能力声明**问题。**在它们被追认之前**，G1 台账里有一批失败**既不能算缺陷、也不能算已解决**。
> 每一项都给出：**被阻塞的条数 → 选项 → 后果 → 建议 → 依据**。**建议不等于决定**；决定权在对应 owner（`kernel/01` owner / 产品与 UX）。
>
> **证据与入口（先读这条链）**：**当前状态与逐项归属**见 `docs/audit/debt-p0.md` 第一段（入口段）；**D-1/D-2/D-3 的数字来源**见 `docs/audit/esctest-classification.md` 与 `docs/audit/esctest-failing-index.md`（机器产物，268/268 定位）；**怎么跑、怎么验**见 `docs/plan/p0-verification-runbook.md`；**D-5 涉及的那条 ADR 分歧**见 `docs/audit/debt-p0.md` 第 142 轮记录；**顶层计划**见 `docs/plan/p0-delivery-plan.md`。
>
> 现状口径：**267 passed / 41 known-bug / 259 failed / substitutions 0**（同一调用、可复现）。

> **⚖ 评审结果（第 256 轮，由 [ADR-0029](../adr/ADR-0029-p0-judging-domain-and-capability-declarations.md) 定案）**：**D-1 / D-2 / D-3 / D-4 全部关闭**，另澄清 D-6 遗留的 **H12 / H13**。
> **其中 D-1 是被「更正前提」关闭的，不是被裁决关闭的**：kernel/01 §5 早已把 **V-02（esctest，100%，`R_strict=1 且 X=0`）** 与 **V-04（xterm 兼容**语料**，`R_gate ≥ 0.99` + 差异上限）** 分成两个套件——本简报把二者当成同一个，因此「哪条 §8 管辖 esctest」不成立。**建议的「读法 B」被否决**：对 esctest 适用差异登记会要求 `X>0`，与 K-01 冲突，且**构成 §8.1-1 的放宽**（需 TSC，而 TSC 未成立）。
> **对 E-P0-1 的影响是收紧**：259 条失败**全部是硬缺口**，不存在「登记豁免」的第三条路；唯一合法的分母削减是 `S_cap`（套件 manifest 的静态能力前置）。

## D-1 `SD-23`：HARNESS §7「esctest 全通过」的**判定域**——**已由 ADR-0029 关闭（第 256 轮）：这不是未决问题，本节的问法前提有误**（**§8 两条同强度条款中哪一条管辖**：套件 100%，还是 xterm ≥99%+差异登记——第 225 轮改）

**被阻塞**：**约 69 条**（未登记扩展族 41 + 查询类 20 + xterm 专有 8），另加颜色 45、`XtermWinops` 19 的归属。

**两种读法**：
| 读法 | 含义 | 后果 |
| --- | --- | --- |
| **A（字面）** | 每条 esctest 用例都必须通过 | 必须实现全部 xterm 扩展；**与我方冻结的 `kernel/01` §3.5「OSC 52 读取永不实现」直接矛盾** |
| **B（子集 + 差异）** | 「全通过」判定在已声明子集上；子集外按 **K-04** 进差异登记（最小复现 + 依据 + 豁免期限） | 上面约 69 条进差异表；**通过率的分母口径必须同时定义** |

~~**建议：采 B。**~~ → **ADR-0029 否决 B**：esctest = kernel/01 **V-02**，`R_strict=1 且 X=0`；「≥99% + 差异登记」属 **V-04（xterm 语料）**。 依据：**A 与自身冻结条款矛盾**——若「全通过」= 全集，则我方已声明的「OSC 52 读永不实现」使 P0 **在构造上不可能达成**。**B 不是降低标准，而是把「不做」变成可审计的登记**（K-04 要求逐条附依据与期限）。

**需要 owner 补的**：① 判定域的明文表述；② **通过率分母口径**（子集内分母 vs 全集分母）；③ K-04 差异表的**双签人**（当前无 TSC，OQ-19 未决）。

**⚠ 第 225 轮：D-1 的**准确形式**（读 HARNESS 原文得出，比「子集 vs 全集」更可裁定）**：**HARNESS §8 第 1 条门禁的原文是**——「**VT 兼容：vttest / esctest / kitty 套件 100%；xterm 兼容用例 ≥99%（差异登记在案）。**」（`HARNESS.md:543`）。**而出口定义那一行写的是**「**vttest + esctest 全通过**」（`:528`）。**关键在于**：**同一句话里对**同一个 esctest** 给了**两条不同强度的要求**——**① 「esctest 套件 100%」（无任何豁免）；② 「xterm 兼容用例 ≥99%，差异登记在案」**。**而 esctest 本身就是 xterm 一致性套件**，**所以 D-1 的真正问题不是「子集还是全集」，而是**：**当 esctest 的失败同时触犯这两条时，哪一条管辖**——**是「套件 100%」（则 259 条失败是硬缺口），还是「xterm 兼容用例 ≥99% + 差异登记」（则差额可进入 K-04 差异表）？** **这仍是一个 §8 门禁的解释问题**（**改变它即改变门禁含义 → 需 TSC，与第 174 轮的分类一致**），**但问题的形状现在可以从权威原文直接读出来，而不是由我概括。**

## D-2 颜色查询的**能力声明**（`OSC 4 / 10 / 11 / 12`）——**已由 ADR-0029 D-2 关闭（第 256 轮）：采 ①（不应答），并在 kernel/01 §3.5 明文登记 + 以 `suites.toml` 的静态能力前置 `S_cap` 排除，不是差异登记**

**被阻塞**：**45 条**（`ChangeColor` / `ChangeDynamicColor` / `ChangeSpecialColor` / `ResetColor` / `ResetSpecialColor`）。

**现状**：这四个序列**不在** `kernel/01` §3.5 子集内；我们**一律不应答**，`esctest` 因此超时失败。

| 选项 | 含义 | 代价 |
| --- | --- | --- |
| **① 不应答（现状）** | 保持沉默 | 45 条按 D-1 的 B 读法进差异表；**代价 0，诚实** |
| **② 声称 xterm 默认 256 色并应答** | 报一套标准调色板 | **在终端尚无颜色渲染时报调色板＝能力声明**；若渲染出来与所报不符，**违反 AR-20 诚实原则** |
| **③ 实现颜色渲染后再应答** | 先有渲染，再谈声明 | 正确顺序，但**属 P1/P2 范围**，不解决 P0 |

**建议：P0 采 ①（保持不应答）**；把「终端调色板是什么」作为**渲染可用之后**的独立决策。**⚠ ADR-0029 更正一处措辞**：①的落地不是「登记差异」（对 esctest 无此路，见 D-1），而是**声明能力缺失**——写进 `kernel/01` §3.5 的「永不（v1）」并经 `suites.toml` 的 `S_cap` 排除。**依据**：AR-20；且**当前没有任何颜色渲染**，报调色板无法自证。

**需要 owner 补的（第 226 轮补写）**：① **P0 是否应答这四个序列**（建议 ① 不应答）；② **「不应答」的记录位置**——**进 K-04 差异表，还是写进 `kernel/01` §3.5 子集清单**（后者更彻底：把「不在子集内」变成明文而非默认）。**⚠ 第 226 轮核实：`OSC 4/10/11/12` 在 `HARNESS.md`、`docs/spec/*`、`docs/spec/kernel/*` 中**零命中**——**即：规范对此完全沉默**，**因此这不是「澄清规范」，而是**一项新的规范决定**（按 §11 的 CR 规则，改变 AR/DC 结论者须走 RFC → 新 ADR）。**

## D-3 `DA` / `DA2` / `DECID` 的**设备身份**——**已由 ADR-0029 D-3 关闭（第 256 轮）**，**⚠ 其「与钉定 xterm oracle 逐字节一致」已于第 257 轮由 [ADR-0030](../adr/ADR-0030-d3-vt-level-and-device-attributes.md) 更正**：DA 断言是**集合包含 + 范围**，级别 5 的 expected 要求声称本实现没有的能力（违反 AR-20）；**改为按规则声明 VT 级别（当前 = 1）并满足该级别断言**（DA1=`CSI ? 1;2 c`、DA2=`CSI > 0;314;0 c`、DECID 依 OQ-VT-14 仅非 UTF-8 模式）

**被阻塞**：**约 4–6 条**（`DATests` 2、`DA2Tests` 2、`DECIDTests` 1；`DECID` 是 DA 的过时形式）。

**现状**：`csi_dispatch` **没有 `CSI c` 分支** → 完全不答设备属性；而 `vte_adapter` 把 `c` 计为「已知」（计数与行为不一致，应顺手修正）。

| 选项 | 含义 | 代价 |
| --- | --- | --- |
| **① 报告自身真实身份**（VT 级别 + 自有扩展） | 诚实 | esctest（`--expected-terminal xterm`）**这 4–6 条仍失败** → 进差异表 |
| **② 声称 xterm 兼容身份** | 可让这几条通过 | **该声明必须有真实兼容度支撑**；当前 esctest 267/259，**尚无资格声称** |

~~**建议：采 ①**~~，并在实现 DA 时**把身份字符串写进 ADR**（对外契约）——**ADR-0029 改采 ②（兼容身份）**：在 V-02 要求 `X=0` 且 esctest 以 `--expected-terminal xterm` 运行的前提下，自有身份使这 5 条**恒红**；诚实义务改由「`DA1` 声称集 ⊆ 实现集」用例 + `TERM_PROGRAM` 承担（AR-20）。**依据**：AR-20；且 ② 的收益（4–6 条）远小于它带来的契约风险。

**需要 owner 补的（第 226 轮补写）**：① **DA/DA2/DECID 应答哪一套身份**；② **该决定记在哪里**（新 ADR，或 `kernel/01` 的对应小节）。**⚠ 第 226 轮查到了**直接先例 K-10**（`docs/spec/kernel/01-vt-conformance.md:90–92`）**：**该项目**已经决定**——`TERM=xterm-256color`（**兼容优先**）+ `TERM_PROGRAM=TermAI`（**在另一个字段里如实自报**）+ `COLORTERM=truecolor`，**并注明「已决（AR-31 第 2 条）」**；**其理由原文是「默认保兼容是入场券」**。**同一处还写着**：**「能力探测（DA1/DA2、`XTGETTCAP`、kitty query）用于进取特性，不用于基础行为」**。**因此 D-3 可以问成一个有先例可依的问题**：**DA/DA2/DECID 应跟随 K-10 的**兼容那一半**（像 `TERM`），还是它的**如实自报那一半**（像 `TERM_PROGRAM`）？**——**而 K-10 里 DA1/DA2 是被**提及**的（作为能力探测），却**没有规定我们应答什么**，所以它仍是新决定，只是有了两条可援引的先例。**

## D-1…D-3 之间的关系（D-4 / D-5 见文末，与本节的取舍无关）

**D-1 是总开关**：它决定 D-2、D-3 的结果是「缺陷」还是「已登记的偏差」。**建议先用一次评审同时定掉 D-1，再据其读法定 D-2/D-3**——否则会出现「同一个东西在两张表里归类不同」的自相矛盾。

## 不在本简报内的两项（属设计，不属判定）

- **`XtermWinops` 19 条**：需要「应用可请求 resize」的**动态 resize + reflow 策略**（连动 `kernel/03`）。**属设计任务**：先定策略（滚动区域/光标/软换行/备用屏在 resize 时的语义），再实现。
- **`DECRQM` 25 条**：**oracle 分歧**（SD-22），需 **xterm 实现或 ctlseqs 原文条款**才能判定；**目前环境拿不到**（网络受限），故**保持 Pm=0 不改**。
## D-4 §8.2 可靠性测量的归属与报告口径（sessiond 重建 P95 ≤2s / P99 ≤5s）——**已由 ADR-0029 D-4 关闭（第 256 轮）：采 A，落点 kernel/06 §3.10 + 独立 `RELIABILITY_MAPPING`（不动 `SECTION5_MAPPING`）**

**被阻塞**：**E-P0-4 的最后一项验收**（AR-26 第 4 条；`HARNESS.md:259`）。

**为什么它是一个决策而不是一个实现**：该验收出自 **§8.2 可靠性**，而 **kernel/06 全文没有出现「重建 / reconnect」**——**§8.2 的时序测量目前没有规定的归属、格式与判定人**。**同时它绝不能进 §5 的注册表**：`tools/bench/registry.mjs` 是 HARNESS §5 十九行的**逐字转录**，**由门禁 B1/B3 每次重新解析文档并逐字比对**，**加一行「重建」既是假转录、也不会被验证**。

| 选项 | 含义 | 代价 |
| --- | --- | --- |
| **A 扩展 kernel/06 覆盖 §8.2** | 在既有方法学内增加一节「可靠性时序测量」，**复用**既有 `bench-report` schema、机器指纹（§3.7）、NON_GATING 语义（ADR-0014）与 AR-27 自证 | **需要一次规范修订并由 owner 追认**；收益是**不出现第二套「什么算有效测量」的定义** |
| **B 另设 §8.2 口径** | 独立格式与流程 | **两套测量规范并存**，日后必然分叉；且要重新回答机器绑定、NON_GATING、可复现性自证等同样的问题 |
| **C 人工流程** | 参考机上按文档化步骤人工测并记录 | **最省事，但 E-P0-4 的「可执行证据」退化为人工记录**，与全项目自动化门禁的口径不一致 |

**建议：A（已采纳：ADR-0029 D-4）。** 依据：**bench 那套（机器指纹、NON_GATING、AR-27 自证、报告 schema）已经存在且被门禁验证**；为 §8.2 **重建一遍**会立刻产生「**两套关于『有效测量』的定义**」——**而这正是本项目最不该出现的分叉**（第 30 轮我建议「测量落 tools/bench」时其实已经直觉地选对了容器，**只是没意识到它需要一个 §8.2 的口子**）。

**需要 owner 补的**：① kernel/06 是否增加 §8.2 一节；② 若增加，**§8.2 行的机器绑定与 NON_GATING 规则**是否与 §5 相同；③ **判定人**（当前无 TSC，OQ-19 未决）。

**⚠ 第 179 轮：D-5 的路径**不适用于本项**——不要照搬**。**D-5 是「实现写在哪里」的归属问题**：工具是 ADR，**无需外部追认，故负责人可自行决定并已在第 177 轮办妥**。**而本项不是归属问题**：A 方案的代价栏明写「**需要一次规范修订并由 owner 追认**」，**其 ① 要求改动 `kernel/06`（规范）**，**③ 指名的追认人正是缺失的 TSC（`C1`：TSC 未成立 → §5/§8 的任何放宽无合法批准人）**。**因此本项不是「找到正确的工具」就能解决，而是**缺一个有权追认的人**——**这是治理缺口（C1），不是技术缺口**。**下一个接手者若试图自行裁定 §8.2 的归属，会重复我在 D-5 上走过的弯路，方向相反**（那次是以为需要人，这次会以为不需要）。

**在定案前**：**不写任何 §8.2 测量代码**，也不得在 `SECTION5_MAPPING` 中新增行（那会破坏 B1/B3 的转录校验）。

**⚠ 第 227 轮：更正 D-4 的**前提**（读 `kernel/06` 目录得出）**：**⚠ 第 229 轮再更正（第 28 次）：上一轮我说「D-4 的问法**一直是**『kernel/06 是否增加 §8.2 一节』」——**这句过宽**。**准确情况是**：**D-4 的**选项表**（本节的方案 A）本来就写对了**——「**扩展 `kernel/06` **覆盖** §8.2**」；**只有「需要 owner 补的」那一行写成了「`kernel/06` 是否增加 §8.2 一节」**（**「§8.2」在 D-4 里指的是**HARNESS/spec 06 的 §8.2 可靠性**，**不是 `kernel/06` 的节号**）。**因此第 227 轮的**实质结论仍然成立**（`kernel/06` 的 §8 是 Open Questions，方法论应落在 §3.10），**但它更正的是**那一行的措辞**，不是整节的问法。** **D-4 的问法一直是「`kernel/06` 是否增加 §8.2 一节」——而 `docs/spec/kernel/06-performance-methodology.md`（386 行）的**§8 是「Open Questions」**（该文件的分节为 §1 范围／§2 关键结论／§3 详细设计（**§3.1 判定状态机、§3.2 §5 逐指标测量方法、§3.3 key-to-photon 探针、§3.4 参考机绑定、§3.5 回归流程、§3.6 可复现性自检、§3.7 harness 规格、§3.8 不能进 CI 的指标、§3.9 §5 指标→测量定义映射表**）／§4 接口／§5 可验证验收／§6 风险／§7 被否决的方案／§8 Open Questions）。**
**因此「§8.2」会落进「Open Questions」之下**——**而 §5 的测量方法本身在 **§3.2 与 §3.9****。**可靠性时序测量（sessiond 重建 P95/P99）与它们同类**，**所以它的自然位置是 **§3 的延续（该节的最后一小节是 §3.9，故为 §3.10）**，不是 §8 的子节。**
**这改变了要 owner 定的东西**：**不是「要不要新开 §8.2」**（那会把方法论放进未决问题节），**而是「是否把可靠性时序测量作为 §3.10 加入既有方法论，并复用 §3.4 的机器绑定、§3.6 的可复现性自检与 `bench-report` schema」**——**后者正是 D-4 的建议 A 想做的事，只是落点应当改。** **（其余待 owner 补的项——§8.2 行的机器绑定是否与 §5 相同、判定人——在第 179 轮的记录中仍然有效。）**

## D-5 §5 测量实现的主场：ADR-0014 的 xtask，还是既有的 Node tools/bench —— **已关闭（`ADR-0028`，第 177 轮）**

**被阻塞**：**E-P0-3 的「§5 每条指标的测量实现」（第 142 轮发现）**。

**事实（已核实）**：**ADR-0014 的实现位置一行写着 `crates/termai-xtask（bench / perf-gate / matrix / dist / sign）`，而该 crate 不存在**（目录不存在、`Cargo.toml` 零提及）；**实际存在且已被门禁验证的是 Node 工具** `tools/bench`（~~B1–B7~~ → **B1–B9（第 257 轮 ADR-0029 D-4 加 B9）** + `--selftest` + `--report` + `--machine`）。

| 选项 | 含义 | 代价 |
| --- | --- | --- |
| **A 以 Node `tools/bench` 为最终主场** | 承认现状 | **需一次新 ADR 取代 ADR-0014 的那一行**（并在旧条目上标注「被 ADR-xxxx 取代」——AGENTS §5 禁止改历史）；**收益是只有一个「什么算有效测量」的定义** |
| **B 新建 `crates/termai-xtask`** | 按 ADR 原计划 | **要重做一遍已被门禁验证的东西**（schema 校验、机器指纹、NON_GATING、AR-27 自证），**且两套实现并存期间必然分叉** |
| **C 测量实现进 xtask、门禁校验留 Node** | 各取一半 | **最危险**：**实现与校验它的门禁分属两处**，正是 D-4 里点名的「两套关于『有效测量』的定义」 |

**建议：A。** 依据：**`tools/bench` 是既存在、又被 CI 逐字校验（B1/B2/B3）且已自证能失败（`bench:selftest`）的那一个**；**B 要求把这份已被验证的工作重做一遍**；**C 会立刻制造 D-4 已经警告过的分叉**。**A 的唯一代价是一次 ADR 更替——而这不是障碍，是本项目处理「推翻旧决策」的标准动作。**

**⚠ 本项与 D-1…D-4 不同类，因此不必等 TSC（第 174 轮辨析）**：**D-1/D-2/D-3 决定「什么算通过」（§7 G1 的判定域、能力声明、设备身份），D-4 决定 §8.2 指标的口径——按 AGENTS §5「指标变更：任何 §5 预算或 §8 门禁的放宽，必须附基准数据与 TSC 批准」，**它们都需要 TSC，而 `C1` 记录 TSC 未成立**，因此这几项是**结构性阻塞**，不是排期问题。**而 D-5 是「实现写在哪里」的归属问题**，**正确工具是 ADR（AGENTS §5 的 ADR 触发条件：新增/删除模块、改变对外契约），不是 §5/§8 的放宽**——**因此它可以由负责人按正常工程程序推进：写一份新 ADR 取代 ADR-0014 的实现位置一行，并在旧条目上标注「被 ADR-xxxx 取代」。** **它一经定案，就解锁 E-P0-3 的「§5 每条指标的测量实现」**——**即：五项决策里有一项不依赖缺失的 TSC，而它恰好挡着一块真实的 P0 工作。**

## D-6 §5 的十九行里，哪几行的实现已经存在（第 180 轮分诊，**不是新决策，是 E-P0-3 的可执行切片**）

**背景**：ADR-0028 已把 §5 测量与门禁的最终主场定为 `tools/bench`。本轮读 `SECTION5_MAPPING`（H1…H19）**只读取、未增行**（该表是 HARNESS §5 的逐字转录，由 B1/B3 校验），按 `family` 与 `machine` 分诊：

| 组 | H 行 | family / machine | 能否现在实现 |
| --- | --- | --- | --- |
| **需要参考机或 UI 宿主** | H1、H5、H6、H7、H8、H9、H10、H11 | startup/throughput/rss/default/exact，**RM-A** | **不能**——依赖 E-P0-2 的窗口/渲染管线与 RM-A 起机 |
| **需要 RM-C** | H2、H3、H4 | frame，**RM-C** | **不能**——同上 |
| **不需要参考机、且是确定性比对** | **H12、H13、H17、H18、H19** | **exact，machine: none** | **其中三项已存在实现，只是没绑到 H 行** |
| **外部/成本** | H14、H15、H16 | external，none | **不能**——依赖 AI 网关与控制面，属 E-P0-2 之后 |

**关键发现**：**`tools/bench` 报「§5 测量实现未做」，但其中数行的实现其实已经存在于别的门禁名下**——

| H 行 | 已有实现（在本仓库中运行） |
| --- | --- |
| **H17 视觉回归 diff** | **视觉基线作业**（ADR-0022 D1/D3，CI 中重生成并按 ADR-0022 上传） |
| **H18 硬编码色值** | **`tokens:check`**（CI 步骤名即含 `hex debt`） |
| ~~**H19 双主题对比度（WCAG）**~~ → **H19 = 网格对齐误差 ≤0.5px；双主题对比度是表外对照 `C1`** | **`tokens:check`**（CI 步骤名即含 `WCAG contrast`）——**第 255 轮更正**：对比度值属 **C1（表外对照，不计入 19 行）**；H19 需 RM-C 的 golden 像素渲染，**本机不可得** |
| H13 安装包 | **无**（打包未做） |
| H12 输入字节等价 | **部分**（`termai-core` 的 `InputEncoder` 有单元测试，未见全语料回放） |

**因此 E-P0-3 的「测量实现」并非从零开始**：**缺口是「这些实现没有被登记为 §5 的测量、也没有绑定到 H 行」**——即 **binding 缺口**，**而 ADR-0028 恰好把 `tools/bench` 定为主场，使这种绑定成为自然动作**。

**代价与前提（如实写明）**：**绑定不等于实现**——**`tokens:check`/视觉基线是门禁，其输出须被整理成 §5 所需的量（H18 是「色值数量为零」这类确定性断言，H17 是像素 diff 阈值）**；**且 `machine: none` 不意味着「与机器无关」，而是「不需要登记的参考机」**（确定性比对）。**在真正登记前，不声称这些 H 行已实现。**


**需要 owner 补的**：① 是否以新 ADR 取代 ADR-0014 的实现位置——**⚠ 第 175 轮核实：ADR-0014 点名 `crates/termai-xtask` 的地方有**两处**，不是一处**：**第 7 行**的「实现位置」行，与**第 285 行**「关联决策与实现位置」表中 `AR-19` 一行的实现位置列（`termai-xtask perf-gate、bench-report.json`）；**新 ADR 必须同时取代这两处**。**编号已定**：当前最大为 ADR-0027（`docs/adr/` 共 28 个文件含 README），**故新 ADR 为 `ADR-0028`**；② 若采 A，**§5 测量的实现是否也随之落到 `tools/bench`**（还是仍需要一个 Rust 侧的性能门禁入口）。

**决策包状态（第 176 轮核实：已完备，只差落笔）**：**选项 A/B/C 与各自代价 ✓、建议与依据 ✓、工具与编号（ADR-0028）✓、需取代的两处（ADR-0014 第 7 行与第 285 行）✓、不依赖 TSC 的辨析 ✓**；**核实方式为对该节逐项字符串核对，非目视**。**`docs/adr/ADR-0028*` 尚不存在**（已 `Test-Path` 确认）——**因此下一步是**：**读 ADR-0014 的决策上下文与 `docs/adr/` 的既有格式，撰写 ADR-0028**（取代上述两处、在旧条目标注「被 ADR-0028 取代」），**随后即可开始 E-P0-3 的 §5 测量实现**。**不要重新推导本节的选项与代价——它们已经定稿。**

**在定案前**：**不写任何 §5 测量代码**（与 D-4 同一条理由）。




**⚠ 第 181 轮对本节的更正（自我更正第 18 次）**：**「已有实现」三行必须按门禁强度区别对待，不能一律说成「已实现」**——**读 `tools/tokens/check.mjs` 第 3 行的自述**：「`// Gates: (1) schema (2) AR-22 rulers (3) WCAG contrast (4) codegen drift (5) hex debt (**warn only**).`」**因此**：
**✅ 第 193 轮：D-6 的可行性确认——步骤 ① 不需要改动任何被校验的 schema**。**已核实**：**`tools/bench/lib.mjs:290` 的 `BENCH_REPORT_SCHEMA`**（kernel/06 §3.7）**其 `metrics` 字段是 `S.arrayOf(METRIC_SCHEMA, 1)`**——**即报告本就承载**一个以上的指标条目**（`:305`）；并且 `:364–389` 保留了对**单指标扁平投影**（`metrics[0].*`）的兼容校验（`LEGACY_METRIC_FIELDS`）。

**第 253 轮：步骤 ① 的**可取得值**已实测（本机）**——**这使该步不必从零开始**：

**✅ 第 254 轮：步骤 ① 已**实做并通过 B8**——不再只是规格**。**做法**：按 `BENCH_REPORT_SCHEMA`（`tools/bench/lib.mjs:290`）写出含 **H18（26）与 H19（5.17）两条 `metrics`** 的报告（**H17 缺失，原因见上**），**再用 `node tools/bench/check.mjs --report <path>` 校验**：

| 尝试 | B8 结果 |
| --- | --- |
| 第一版 | **FAIL，4 处违规**：`selfcheck` 的三处 verdict 写成 `SKIP`（**该枚举只许 `PASS\|FAIL\|INCONCLUSIVE\|INVALID`**）；**且缺必填字段 `cost`** |
| 修正后 | **PASS**——「report satisfies kernel/06 3.4 / 3.7 schemaVersion 1.0.0」 |

**因此实现步骤 ② 的人不必再踩这两处**：**① `selfcheck.*.verdict` 用 `INCONCLUSIVE`（不是 `SKIP`——`SKIP` 只存在于**指标**级枚举）；② `cost` 是必填对象 `{ minutes, runnerClass, estUsd }`（在 schema 末尾，容易漏读）。** **示例报告在 `target/bench/h17-h18-h19-partial.json`（构建目录、不入库）**；**其存在证明：本机可为 H18／H19 产出**符合 kernel/06 3.7 的报告**。**

**⚠ 第 255 轮更正第 253／254 轮的两处标注（这是本轮最要紧的更正）**：**第 254 轮那份报告把 WCAG 对比度 5.17 标成了 metric `H19`——而 H19 是「网格对齐误差 ≤0.5px」（`unit: px`、`gate: 0.5`，判据 kernel/03 RP-05 / AR-24 第 3 条）**。**证据是权威原文，不是我的推断**：`HARNESS.md:485` 的 §5 行是「网格对齐误差 ｜ ≤0.5px ｜ — ｜ 渲染用例」，`docs/spec/kernel/06-performance-methodology.md:317` 的 H19 行同样写「网格对齐误差 ≤0.5px」，而**双主题对比度在 kernel/06 §3.9 里是**表外对照 `C1`**（该文件 :318，且注「非 §5 表行」）——**HARNESS §5 根本没有对比度这一行**。**因此 5.17 的正确身份是 `C1`，不是 H19。** **同时 `H18` 的值也错了**：`tokens:check` 第 5 关对**三个** HTML 文件给数（netcatty **27** / prototype **26** / terminal-first **0**），**第 253 轮只取了 prototype 一个文件的 26**——**§5 门禁写的是「= 0」，它不是某一个文件的属性。因此 H18 的量是 53。** **这条更正的直接后果**：**第 254 轮那份示例报告现在会被 `B8` 判 FAIL**（`H19: unit mismatch -- HARNESS section 5 says "px" but the report says "ratio"`；`H19: gate mismatch -- the registry says 0.5 but the report says 4.5`）——**而这是正确的行为**：**一个名字与内容不符的值不该被发表**。**修正后的示例报告 `target/bench/machine-free-values.json`（H18=53、C1=5.17、H17/H19 如实缺席）通过 `B8`。** **本机因此可产出的 §5 行只有 H18 一项，另加表外对照 C1——H17 需浏览器层、H19 需 RM-C 像素渲染。**

| 行 | 值从哪来 | **本机实测结果** |
| --- | --- | --- |
| **H18 硬编码色值** | `tokens:check` 第 5 关 | ~~**26 处硬编码色值（17 个不同）**，位于 `termai-ui-prototype.html`~~ → **第 255 轮更正：53 处**（三个 HTML 文件：netcatty 27 / prototype 26 / terminal-first 0；该关 warn-only，故 `result: PASS`） |
| ~~**H19 双主题对比度**~~ → **C1 双主题对比度（表外对照，不是 H19）** | `tokens:check` 第 3 关（真实 WCAG 公式） | **逐对给出比值**：`ghost-on-term-bg` 5.17／5.89、`text3-on-surface` 6.35、`text3-on-sunken` 5.47／6.66、`diff-ctx-on-sunken` 5.47／6.66——**均 `>=4.5`**；**C1 要记录的量应是**所有对中的最小值**（深／浅两主题合取 = **5.17**），可从该关完整输出取得** |
| **H17 视觉回归** | `tools/design-gates` 的 B4 | **本机取不到**（**浏览器层需要 Chrome**——第 250／252 轮实测：`--static` 下 10 关 SKIP，完整检查因无法比对而 FAIL） |

**因此步骤 ① 在本机**只完成三分之一**（**第 255 轮更正**）：**H18 与表外对照 C1 的值可直接从 `tokens:check` 取得**（**且该工具已在 CI 中运行**）；**H17 需 CI 的 design job 或一台有浏览器的机器**；**H19（网格对齐误差）需 RM-C 的 golden 像素渲染，本机既无渲染管线也无 RM-C**。

**因此**：~~**为 H17/H18/H19 产出一份含五个指标条目的 `bench-report.json`**~~ → **为机器无关行（第 255 轮实为 H18 + 表外对照 C1 两个条目）产出一份 `bench-report.json`** 是**现成格式**，不需要动 schema**——**B1–B3 逐字校验的是 schema 与 HARNESS §5 的转录，而这份报告填的是 schema 已允许的字段**。**这也让任务边界更干净**：**不碰任何被门禁校验的东西，只新增一条读取路径。**

**D-6 的可行性至此闭合**：

| 步骤 | 可行性 |
| --- | --- |
| ① 值的来源（写出 `bench-report.json`） | ✅ **schema 已支持多指标条目，无需改动** |
| ② 值的进入（读取路径） | ~~❌ **要建的东西**（`--report` 现只校验 schema）~~ → **✅ 第 255 轮已落地**：`tools/bench/values.mjs` + `B8` 扩展（schema 校验 + §5 行绑定） |
| ③ 值的呈现（标 NON_GATING） | ~~❌ 缺~~ → **✅ 第 255 轮已落地**：机器无关行逐行呈现，**缺席者打印 `NOT REPORTED`（显式，不静默省略）**；表外对照 `C1` 单独一行 |
| ④ 计数器（从常量改为计算值） | ~~❌ 缺~~ → **✅ 第 255 轮已落地**：`gatingNumbersProduced` 由读取到的、声明 `gating:true` 的 metric 计算 |
| ⑤ 验收（注入 + 对照 + 注入 gating 行使 `B7` FAIL） | ~~❌ 待写~~ → **✅ 第 255 轮已落地**：`bench:selftest` **63/63**（第 256 轮经 ADR-0029 D-5 增为 **65/65**、第 257 轮经 ADR-0029 D-4 增为 **74/74**），含「去掉 H18 → 显式未报告」「H19 被误标 → unit+gate 不匹配」「`gating:true` → B7 FAIL」及其对照 |

**结论**：**这是一件「不改动已冻结契约、只新增读取与呈现」的工作**——**在 P0 的出口里，这种性质的工作不多，而这也是它值得被排在「可立即开工」的原因。**
**✅ 第 190 轮：D-6 的最后一处未知已查清——「值」的进入路径不存在，这是任务真正要建的东西**。**已核实**：**`--report <p>` 的用途在 `check.mjs:9` 的自述里写得很清楚**——「**additionally schema-validate a bench-report.json**」——**即：它**只做 schema 校验**，**不读取其中的测量值**（`:928`/`:927` 解析参数，`:696` 遍历 `status.reports` **只取 parseError 与校验错误**）。**结合前几轮**：**`bench` 能做的三件事是**——**① 校验报告的 schema（B8/B1–B3）**、**② 用合成夹具评估判定逻辑（B6）**、**③ 声明不产出 gating 数字（B7，其判据已死）**。**它不能做的事是**：**接收真实的测量值，并把它们作为 NON_GATING 列出来。**

**因此 D-6 的任务至此完全清楚（这是第 190 轮的结论，也是本线的收束）**：

| 步骤 | 内容 | 已有基础 |
| --- | --- | --- |
| **① 值的来源** | 为 H17/H18/H19 产出值（H17←`tools/design-gates`（需浏览器）、H18←`tokens` 第 5 关（**53**）、~~H18/H19←`tokens`~~ **C1←`tokens` 第 3 关（5.17，表外对照）**；**H19←design-gates（需 RM-C 像素渲染，本机无）**），**写成一份 `bench-report.json`**——**schema 已经存在且被 B1–B3 逐字校验**，**所以这是复用而非新建格式** | ✅ schema 与校验在；**第 255 轮实做**：`target/bench/machine-free-values.json` 过 `B8` |
| **② 值的进入** | **`bench` 需要一条「读取报告中的值」的路径**——**而 `--report` 现在只校验 schema**，**所以这条路径要新建** | ❌ 缺 |
| **③ 值的呈现** | 把五行标为 **NON_GATING** 列出 | ❌ 缺（第 184 轮已核实 JSON 里无逐行值） |
| **④ 计数器** | **把 `gatingNumbersProduced` 从常量改为从结果计算**——**否则 `B7` 的沉睡判据不会醒来** | ❌ 缺（第 188 轮已核实） |
| **⑤ 验收（规则 10）** | **注入**：五行中一行的值缺失 → 报告须显式说明；**对照**：补齐后值须出现；**另加**：注入一个产出 gating 数字的行 → `B7` 须判 FAIL | ❌ 待写 |

**「复用 schema、新建读取路径」是这一步最省的做法**——**而它之所以是最省的，恰恰因为第 190 轮查清了 `--report` 现在只做校验。**
**⚠ 第 189 轮：更正上一轮的说法——`B7` **不是**整体恒真，恒真的只是它的「诚实边界」那一条（自我更正第 22 次）**。**第 188 轮我写「`B7` 当前是恒真的」，这句过强。逐条读它的三个判据**：

| `B7` 的判据 | 是否活 | 证据 |
| --- | --- | --- |
| **`if (status.gating)` 报「本机声称 gating」** | **活** | **`:668` 的 `status.gating` 来自 `:662` 的 `B.classifyMachine(machine)`**——**而 `lib.mjs:711` 会在「参考机在 T0」时返回 `gating: true`**；**`fixtures.mjs:111` 有 `gating: true` 的夹具**；**selftest `:894`/`:898` 断言了分类结果** |
| **`noFp` 判「无指纹的机器必须是 INCONCLUSIVE(REFERENCE_MACHINE_UNAVAILABLE)」** | **活** | `:684` 调 `classifyMachine({ fingerprintRecorded: false })`，**与 `lib.mjs:697` 的实际返回值比对** |
| **`gatingNumbersProduced !== 0`（诚实边界本身）** | **❌ 死** | **计数器在 `:672` 是字面常量 0**（第 188 轮已核实） |

**因此准确表述是**：**`B7` 的两条判据是活的，第三条（也就是「无参考机即不得产出 gating 数字」这一条）是恒真的**——**即：`B7` 会真实地检查机器分类，但它**不会**检查「是否偷偷产出了 gating 数字」。**

**对 D-6 的影响因此收窄（而非扩大）**：**任务不是「修 `B7`」**，**而是把 `gatingNumbersProduced` 从常量改成一个从结果算出来的值**——**一旦它开始被计算，`B7` 那条沉睡的判据就会自己醒来**。**规格里的第四条验收条件据此改写为**：「**实现时必须让计数器来自结果，并注入一个产出 gating 数字的行以确认 `B7` 因此判 FAIL**」。
**⚠ 第 188 轮：`B7` 目前是一个**不可能失败**的门禁——`gatingNumbersProduced` 被硬编码为 0（自我更正第 21 次，且这是本线上最要紧的一处）**。**全 `tools/bench` 内 `gatingNumbersProduced` 只有 5 处命中，全在 `check.mjs`**：**`:672` 是**赋值**（`gatingNumbersProduced: 0,`——**字面常量**）**；`:688` 是 `B7` 的判据（非 0 即 FAIL）；`:729`/`:754` 是打印与 JSON 输出；`:901` 是 selftest 的对照断言（`=== 0`）。**即：**没有任何地方**从测量结果**计算**这个计数——它是一个常量。**

**由此得到三条结论，其中两条推翻我前面的说法**：

| # | 结论 |
| --- | --- |
| **①** | **`B7` 当前是恒真的**：它检查 `!== 0`，而被检查的值永远是 0——**它无法发现任何「真的产出了 gating 数字」的运行**。**这正是本会话反复抓到的那类门禁（第 160 轮 K4 的 AR-03、第 133 轮 ci-cost 的上限分支）：通过，但不可能失败。** |
| **②** | **我第 183 轮说「`B7` 禁止在本机产出 gating 数字」——需更正为**：「`B7` **打算**禁止，**但目前还没有能力禁止**，因为计数器是常量。」**它现在是**声明**，不是**门禁**。** |
| **③** | **第 187 轮那条「H13 与 `B7` 的张力」因此**尚未成立**，但它会在**测量实现落地的那一刻**成立**——**因为那时计数器才第一次需要被计算**。**所以 H13 的问题不是现在要答的，而是**实现计数器时要答的第一道题**。** |

**对 D-6 任务规格的影响（重要）**：**「产出并标注 NON_GATING 值」这件事，必须**同时**把 `gatingNumbersProduced` 从常量改为**从结果计算**——**否则 `B7` 会继续恒真，而这个「诚实边界」只是一个被打印出来的 0。** **并且：计数器的计算逻辑本身需要注入 + 对照**（规则 10）——**例如「注入一个 gating 行并令其产出数字 → `B7` 必须判 FAIL」**。**这是任务规格里此前缺失的一条。**
**⚠ 第 187 轮：本任务的五行**不齐质**——`governed` 有三种取值，而我的规格把它们当成同一种（自我更正第 20 次）**。**逐行核实结果**：

| 行 | `governed` | 含义 |
| --- | --- | --- |
| **H12** 输入字节等价 | **`partial`** | **部分受门禁**（不是我写的 `no`）——与它「已有 `InputEncoder` 单测、无全语料回放」的现状一致 |
| **H13** 安装包 | **`yes`** | **它是门禁行，却 `machine: none`** |
| H17 视觉回归 / H18 硬编码色值 / ~~H19 对比度~~ **C1 对比度（表外对照）** | `no` | 非门禁 |

**由此产生一个必须先解决的问题，而它不在实现者权限内**：**`B7`（`check.mjs:688`）的判据是 `status.gatingNumbersProduced !== 0` → FAIL**。**H13 是 `governed: yes` 的行**——**若它的数字被计为 gating，则在本机产出它就会让 `B7` 判 FAIL**；**而 H13 又是 `machine: none`，本不需要参考机**（`check.mjs:415` 的注释正写着 `// package bytes: not a machine gate`，说明工具侧已经知道这一点）。**即：一条「需要产出 gating 数字、却不需要参考机」的行，与一条「无参考机即禁止任何 gating 数字」的边界规则之间，存在需要澄清的张力。**

**处置**：**登记为需要澄清的问题，而不是由实现者猜**——**可能的解释有二**：① `B7` 的「gating 数字」**仅指机器绑定的门禁数字**（则 H13 不受限，注释已暗示此意）；② **`B7` 是保守规则，任何 gating 数字都需机器出证**（则 H13 在本机不可产出，**这与 §5 把 H13 列为门禁行存在冲突**）。**在澄清之前，D-6 的可立即开工范围应缩到 `governed: no` 的三行（H17/H18/H19）**——**H12 需先明确 `partial` 的含义，H13 需先解决上述张力。**
**⚠ 第 185 轮：本任务必须自带「注入 + 对照」，否则它会成为下一个假绿（§6.3 规则 10）**。**理由是第 160 轮那次亲身经历**：**我给 `K4` 加的 AR-03 检查，其违规分支因为 TDZ 而**永远不可能执行**，却一直报 PASS**——**因为「没有命中」时那行代码根本不被求值**。**而本任务要加的是一条**新的呈现路径**：它完全可能「什么都不报」而仍然通过所有既有门禁**（`B1–B8` 检查的是结构与口径，**不会检查「NON_GATING 值是否真的被列出来了」**）。

**因此规格追加一条验收要求**：

| 要求 | 内容 |
| --- | --- |
| **注入** | **人为使五个 machine-free 行中的一行的值不可得**（例如让 `tokens` 的输出缺失），**报告必须显式反映该行的缺失**，**而不是静默省略** |
| **对照** | **同一输入补齐后，该行的 NON_GATING 值必须出现**——**以排除「它只是什么都不报」** |
| **不得越过 `B7`** | 上述两种情形下 `gatingNumbersProduced` **都必须保持 0** |

**这三条与 `kernel-gates --selftest`（~~23/23~~ **24/24**）、`bench:selftest`（~~65/65~~ **74/74（第 257 轮）**）、`conformance selftest`、`ci-cost --selftest` 是同一形态**：**新增呈现路径必须被证明「能报出它该报的东西」，而不只是「跑得通」**。
**✅ 第 184 轮：E-P0-3 在本机的可执行任务，现在是完全指定的（D-6 的收束）**。**已核实**：`node tools/bench/check.mjs --json` 目前**只报状态与计数**——`"state": "INCONCLUSIVE"`、`"gatingNumbersProduced": 0`、`"detail": "no reference machine on this host: every discovered value is NON_GATING / INCONCLUSIVE and 0 gating…"`——**没有任何逐行的 NON_GATING 值**（JSON 里不存在 `values` / `nonGating` 数组）。

**因此任务定义为**：

| 项 | 内容 |
| --- | --- |
| **做什么** | **为 `machine: none` 且 `governed: no` 的五行（H12/H13/H17/H18/H19）产出并**标注 NON_GATING** 的值**，使 `bench` 的报告里出现这五行的数字（**而不是 gating 数字**） |
| **输入来自哪里** | **H17 ← `tools/design-gates`**（`registry.owner` 已如此标注；其阈值默认 `maxDiffPct=0`，即零个不同像素；**本机需浏览器层**）；**H18 ← `tokens:check` 第 5 关 hex debt（本机可得 = 53，warn-only）**；~~**H18/H19 ← tokens:check**（现为 hex warn-only + WCAG 对比度真实计算）~~ **C1（表外对照）← `tokens:check` 第 3 关 WCAG（本机可得 = 5.17）**；**H19 ← `tools/design-gates` 的 RM-C golden 像素测量（本机不可得）**；**H12 ← `termai-core` 的 `InputEncoder`**（现仅单测）；**H13 ← 无**（打包未做） |
| **硬边界（不可越过）** | **`B7` 要求本机 `gatingNumbersProduced === 0`**——**产出任何 gating 数字即判 FAIL**。**因此实现必须是「NON_GATING 值的呈现」**，**且不得让 `B7` 的数字变为非零** |
| **副产品** | **H18 若要从「只警告」变成「阻断」，那是**收紧门禁**（不需 TSC），但**会改变 CI 行为**，须单独记录**——**不属于本任务，但本任务会暴露它** |
| **不在本任务内** | **H1–H11（需 RM-A/RM-C 与 UI 宿主）**、**H14–H16（外部）**——**它们在 `B7` 与 E-P0-2 的限制下不可能在本机产出** |

**这条定义是第 180–184 五轮核实的产物**；**它把一个「§5 测量实现未做」的笼统缺口，收敛成一个有输入、有边界、有副产品的具体任务**。
**⚠ 第 183 轮：读 B7 之后，本节的任务定性必须再改一次——「让 bench 产出 §5 数字」是一个**不可能也不允许**的工作**。**`tools/bench/check.mjs:676` 的 B7 门禁是「机器绑定诚实边界」，其判据在 `:688`**：**`if (status.gatingNumbersProduced !== 0) problems.push('this tool produced …')`**——**即：本机没有参考机（RM-A/RM-C）时，工具**必须产出零个 gating 数字**；产出了就判 FAIL**。**其 PASS 文案（`:704`）也写着「no reference machine on this host: every discovered value is NON_GATING」。**

**因此正确的任务定性是**：

| 行 | 能否在本机产出**gating** 数字 | 能否产出**NON_GATING** 值 |
| --- | --- | --- |
| **H1–H11（machine: RM-A/RM-C）** | **不能，且被 `B7` 禁止**（产出即 FAIL）——**这是 ADR-0014 的有意设计，不是缺陷** | 可以（标注 NON_GATING） |
| **H12、H13、H17、H18、H19（machine: none）** | — | **可以，且它们的 `governed` 本就是 `no`**（非门禁） |

**所以 E-P0-3 的「§5 测量实现」在本机的可执行形态是**：**为 machine-free 的五行产出并标注 NON_GATING 的值**，**而不是「让 bench 报出 gating 数字」**——**后者在任何没有 RM-A/RM-C 的机器上都是被 `B7` 主动拒绝的**。**一个按「让 bench 报数字」派下去的工单会在 `B7` 前撞墙，而撞墙的原因写在 ADR-0014 里。**

**这条更正同时解释了第 180 轮那句「`gating numbers produced: 0`」**：**它不是「没做」，而是**系统按设计拒绝在没有参考机时给出 gating 数字**——**以及没有把 machine-free 的五行按 NON_GATING 报出来**。
**⚠ 第 182 轮：完成 H17 的核实，并再次收窄本节的判断（自我更正第 19 次）**。**① H17 的门禁值不等于 §5 的 0.1%，而是**更严**：**`tools/design-gates/browser.mjs:454`**——**`maxDiffPct` 的默认值是 `0`**（`opts.maxDiffPct === undefined ? 0 : …`），**而 `:720` 的判定是**：**`maxDiffPct > 0 ? d.diffPct <= maxDiffPct : d.diffPixels === 0`**——**即默认以「零个不同像素」为准，严格于 §5 的 ≤0.1%**。**且它确实阻断**（超限即失败，`:736`）。**因此 H17 不是弱项，而是超集**。

**② 更重要：「绑定缺口」比我第 180 轮说的要小**。**H17 在 `registry.mjs` 里的 `owner` 字段写的就是 `tools/design-gates`、`carrier` 写的就是 `design:check`**——**即：H 行与实现工具之间的对应关系，本来就记在 `owner` / `carrier` 两个字段里**。**我第 180 轮说「实现没有被绑定到 H 行」，这一句需要收窄**：**绑定信息存在于登记表，缺的是「`tools/bench` 把这些 H 行作为 §5 测量报告出来」**（这正是 `gating numbers produced: 0` 的含义）。

**连续三轮的判断都在收窄**：第 180 轮说「已有实现」→ 第 181 轮改为「已有测量、强度不一」→ **第 182 轮再改为「H17 强度更高、且绑定信息本就在 `owner`/`carrier` 里，真正缺的是 bench 侧的登记与报告」**。**三次都是同一个动作在起作用：去读那个工具，而不是相信自己上一轮的概述。**

| H 行 | 更正后的准确状态 |
| --- | --- |
| **H18 硬编码色值** | **扫描存在，但门禁是 `warn only`**——**它观察，不阻断**。**§5 的 H18 是 `family: exact`**，**因此「有扫描」≠「有等价强度的实现」**；**绑定之外还须决定是否把 warn 提升为 fail**（**收紧门禁不是放宽，不需要 TSC；但它改变 CI 行为，须记录**） |
| ~~**H19 双主题对比度**~~ → **C1（表外对照）双主题对比度** | **门禁列（正文 ≥4.5:1）确已强制**（`thresholds = { text: 4.5, ui: 3.0 }`，真实计算）；**目标列（官方主题 ≥7:1）未被强制**——**而按 HARNESS §5 的「门禁 / 目标」两列口径，目标列本就不阻断，故此点正确，不是缺口** |
| **H17 视觉回归 diff** | 视觉基线作业（ADR-0022）**阻断**；**本轮未核实其阈值是否等于 H17 的门禁值**——**留待核实，不预先声称等价** |

**本节此前的「已有实现」是**粗判**；**准确表述是「已有测量，强度不一」**——**这正是第 165 轮那条教训的又一次应用：按名字找到的东西，不等于具备了它名字所声称的能力**。
